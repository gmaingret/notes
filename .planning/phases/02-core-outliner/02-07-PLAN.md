---
phase: 02-core-outliner
plan: "07"
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - client/src/components/DocumentView/BulletNode.tsx
  - client/src/components/DocumentView/BulletTree.tsx
  - client/src/components/DocumentView/ContextMenu.tsx
  - client/src/components/DocumentView/DocumentToolbar.tsx
autonomous: true
requirements: [BULL-11, BULL-12, BULL-13, BULL-14, BULL-15]

must_haves:
  truths:
    - "Right-clicking a bullet opens a context menu with: indent, outdent, move up, move down, complete, delete"
    - "Context menu Escape or outside-click closes it"
    - "Clicking 'complete' marks bullet with strikethrough + 50% opacity; clicking again unmarks it"
    - "Clicking 'delete' soft-deletes bullet (it disappears from tree; undo restores it)"
    - "'Hide completed' toggle in toolbar filters completed bullets from view (client-side only)"
    - "'Delete completed' button in toolbar permanently hard-deletes all completed bullets in current document (with confirmation)"
    - "Completed bullets stay in tree position when not hidden (not moved to bottom)"
  artifacts:
    - path: "client/src/components/DocumentView/ContextMenu.tsx"
      provides: "Right-click context menu with all bullet actions"
    - path: "client/src/components/DocumentView/DocumentToolbar.tsx"
      provides: "Document-level toolbar: Hide completed toggle, Delete completed button"
    - path: "client/src/components/DocumentView/BulletNode.tsx"
      provides: "onContextMenu handler opening ContextMenu, complete strikethrough styling"
    - path: "client/src/components/DocumentView/BulletTree.tsx"
      provides: "hideCompleted state passed to BulletNode; toolbar rendered above tree"
  key_links:
    - from: "client/src/components/DocumentView/ContextMenu.tsx"
      to: "client/src/hooks/useBullets.ts"
      via: "useSoftDeleteBullet, useMarkComplete, useIndentBullet, useOutdentBullet, useMoveBullet mutations"
    - from: "client/src/components/DocumentView/DocumentToolbar.tsx"
      to: "/api/bullets/documents/:docId/completed"
      via: "DELETE request via apiClient for bulk hard-delete (BULL-14)"
---

<objective>
Add the remaining bullet interaction layer: right-click context menu (BULL-15), complete/delete from UI (BULL-11, BULL-12), hide-completed toggle and bulk-delete toolbar (BULL-13, BULL-14). These are all Wave 4 — they depend on the full DnD and zoom tree from Plans 05+06.

Purpose: Close out all bullet interaction requirements before the deploy+verify checkpoint.
Output: Context menu, toolbar, complete/delete visual states.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-outliner/02-CONTEXT.md
@.planning/phases/02-core-outliner/02-05-SUMMARY.md
@.planning/phases/02-core-outliner/02-06-SUMMARY.md
@client/src/components/DocumentView/BulletNode.tsx
@client/src/components/DocumentView/BulletTree.tsx
@client/src/hooks/useBullets.ts

<interfaces>
<!-- From existing hooks (Plan 04) -->
useSoftDeleteBullet() → mutate({ id: string })
useMarkComplete() → mutate({ id: string, isComplete: boolean })
useIndentBullet() → mutate({ id: string })
useOutdentBullet() → mutate({ id: string })
useMoveBullet() → mutate({ id: string, newParentId: string|null, afterId: string|null })

Bullet.isComplete: boolean — drives strikethrough + 50% opacity styling (per REQUIREMENTS.md BULL-12)
Bullet.deletedAt: null means visible; non-null means soft-deleted and hidden from tree
BULL-14 delete endpoint: DELETE /api/bullets/documents/:docId/completed — permanently purges completed bullets
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContextMenu component + BulletNode right-click</name>
  <files>client/src/components/DocumentView/ContextMenu.tsx, client/src/components/DocumentView/BulletNode.tsx</files>
  <action>
Create `client/src/components/DocumentView/ContextMenu.tsx`:

Props: `{ bullet: Bullet; bulletMap: BulletMap; position: { x: number; y: number }; onClose: () => void }`

Render a fixed-position div at `{ top: position.y, left: position.x }` with z-index 1000. Menu items (each a `<button>` with onClick that calls the mutation then `onClose()`):
- **Indent** — calls `useIndentBullet` → disabled if bullet has no previous sibling (first sibling)
- **Outdent** — calls `useOutdentBullet` → disabled if bullet.parentId is null (root level)
- **Move Up** — calls `useMoveBullet` with afterId of the sibling before the previous sibling
- **Move Down** — calls `useMoveBullet` with afterId of the next sibling
- **separator line**
- **Mark complete** / **Unmark complete** — label toggles based on `bullet.isComplete`; calls `useMarkComplete`
- **Delete** — calls `useSoftDeleteBullet`; text color red

Close triggers:
1. `useEffect` that adds `mousedown` listener on `document`; if click target is outside the context menu div, call `onClose()`
2. `useEffect` that adds `keydown` listener; if `e.key === 'Escape'`, call `onClose()`

Both effects cleaned up on unmount.

Styling: white background, 1px solid #e0e0e0 border, border-radius 4px, box-shadow, min-width 160px, padding 4px 0. Each button: display block, width 100%, padding 6px 12px, text-align left, no border, background transparent, cursor pointer. Hover: background #f5f5f5. Disabled: color #ccc, cursor default.

Update `client/src/components/DocumentView/BulletNode.tsx`:
- Add state: `contextMenuPos: { x: number; y: number } | null = null`
- Add `onContextMenu` to the outer div: `e.preventDefault(); setContextMenuPos({ x: e.clientX, y: e.clientY })`
- Render `{contextMenuPos && <ContextMenu bullet={bullet} bulletMap={bulletMap} position={contextMenuPos} onClose={() => setContextMenuPos(null)} />}` at the bottom of the BulletNode return
- Add complete styling: if `bullet.isComplete`, apply `{ opacity: 0.5, textDecoration: 'line-through' }` to the BulletContent container
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit 2>&1 | grep -E "ContextMenu|BulletNode|error" | head -10</automated>
  </verify>
  <done>ContextMenu.tsx compiles. BulletNode has onContextMenu handler. Completed bullets show strikethrough + 50% opacity styling.</done>
</task>

<task type="auto">
  <name>Task 2: DocumentToolbar (hide completed / delete completed)</name>
  <files>client/src/components/DocumentView/DocumentToolbar.tsx, client/src/components/DocumentView/BulletTree.tsx</files>
  <action>
Create `client/src/components/DocumentView/DocumentToolbar.tsx`:

Props: `{ documentId: string; hideCompleted: boolean; onToggleHideCompleted: () => void }`

Renders a slim toolbar row above the bullet list:
```tsx
<div style={{ display: 'flex', gap: 8, alignItems: 'center', padding: '4px 0', borderBottom: '1px solid #f0f0f0', marginBottom: 8 }}>
  <button onClick={onToggleHideCompleted} style={{ fontSize: '0.8rem', color: hideCompleted ? '#4A90E2' : '#999', background: 'none', border: 'none', cursor: 'pointer' }}>
    {hideCompleted ? 'Show completed' : 'Hide completed'}
  </button>
  <button onClick={handleDeleteCompleted} style={{ fontSize: '0.8rem', color: '#e55', background: 'none', border: 'none', cursor: 'pointer' }}>
    Delete completed
  </button>
</div>
```

`handleDeleteCompleted`: Show `window.confirm('Permanently delete all completed bullets? This cannot be undone.')`. If confirmed: `await apiClient.delete('/api/bullets/documents/' + documentId + '/completed')` then `queryClient.invalidateQueries({ queryKey: ['bullets', documentId] })`.

Update `client/src/components/DocumentView/BulletTree.tsx`:
- Add `hideCompleted` state (useState false)
- Render `<DocumentToolbar documentId={documentId} hideCompleted={hideCompleted} onToggleHideCompleted={() => setHideCompleted(h => !h)} />` above the SortableContext
- Pass `hideCompleted` into flattenTree: modify `getChildren` call to also filter out `b.isComplete` when `hideCompleted` is true — OR filter the flatItems array after flattenTree. Simplest: `const visibleItems = hideCompleted ? flatItems.filter(b => !b.isComplete) : flatItems`
- Note: hiding completed bullets may expose children of completed parents — acceptable UX behavior for this scope (children of hidden-completed parent appear at parent's former depth)
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit 2>&1 | grep -E "DocumentToolbar|BulletTree|error" | head -10</automated>
  </verify>
  <done>DocumentToolbar.tsx compiles. BulletTree renders toolbar. Hide completed filters bullet list. Delete completed sends DELETE to /api/bullets/documents/:docId/completed after confirmation.</done>
</task>

</tasks>

<verification>
`cd client && npx tsc --noEmit` — no errors.
`cd server && npx vitest run && cd ../client && npx vitest run` — all tests green.
After deploy: right-click bullet → context menu appears. Click 'complete' → bullet gets strikethrough + faded. Click 'delete' → bullet disappears from tree. 'Hide completed' toggle hides/shows completed bullets. 'Delete completed' shows confirmation, then purges on confirm.
</verification>

<success_criteria>
- Right-click on any bullet opens context menu at cursor position
- Context menu has: indent, outdent, move up, move down, (separator), mark/unmark complete, delete
- Clicking outside or pressing Escape closes context menu
- Completed bullets show strikethrough text + 50% opacity, stay in position
- 'Hide completed' toggle filters completed bullets client-side only (no API call)
- 'Delete completed' calls DELETE /api/bullets/documents/:docId/completed with browser confirm; purges permanently
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-outliner/02-07-SUMMARY.md`
</output>
