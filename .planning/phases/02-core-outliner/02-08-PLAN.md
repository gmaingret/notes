---
phase: 02-core-outliner
plan: "08"
type: execute
wave: 5
depends_on: ["02-07"]
files_modified: []
autonomous: false
requirements: [BULL-01, BULL-02, BULL-03, BULL-04, BULL-05, BULL-06, BULL-07, BULL-08, BULL-11, BULL-12, BULL-13, BULL-14, BULL-15, KB-01, KB-02, KB-03, KB-04, KB-05, KB-06, KB-07, UNDO-01, UNDO-02, UNDO-03, UNDO-04]

must_haves:
  truths:
    - "Full test suite passes: cd server && npx vitest run && cd ../client && npx vitest run"
    - "Production is live at https://notes.gregorymaingret.fr with bullet editor visible"
    - "User-facing bullet tree is functional for all Phase 2 requirements"
    - "Undo history survives page refresh (confirmed by testing)"
  artifacts:
    - path: ".planning/phases/02-core-outliner/02-08-SUMMARY.md"
      provides: "Phase 2 completion record"
  key_links:
    - from: "https://notes.gregorymaingret.fr"
      to: "192.168.1.50:8000"
      via: "Nginx reverse proxy (already configured from Phase 1)"
---

<objective>
Deploy Phase 2 to production, run the full test suite, and have the user verify all bullet behaviors in the browser. This is the only human checkpoint in Phase 2 — it gates phase completion.

Purpose: Confidence that the bullet editor works correctly in production before Phase 3 begins.
Output: Production deployment confirmed; Phase 2 signed off.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-outliner/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + deploy to production</name>
  <files></files>
  <action>
Run full test suite locally first:
```bash
cd /c/Users/gmain/Dev/Notes/server && npx vitest run
cd /c/Users/gmain/Dev/Notes/client && npx vitest run
cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit
```

Fix any failures before deploying.

Then deploy following the MEMORY.md standard workflow (STRICT ORDER):
1. `git add` all Phase 2 files, `git commit`, `git push origin phase-2/core-outliner` (or current branch)
2. SSH deploy: `ssh root@192.168.1.50 "cd /root/notes && git fetch && git checkout phase-2/core-outliner && docker compose up -d --build"`
3. Tail logs to confirm startup: `ssh root@192.168.1.50 "cd /root/notes && docker compose logs --tail=50 app"`
4. Verify the app is reachable: the production URL is https://notes.gregorymaingret.fr (Nginx proxy) or http://192.168.1.50:8000 (direct LAN)

CRITICAL REMINDER from MEMORY.md:
- Do NOT restart Docker daemon or reboot the machine (other containers run there)
- Only allowed Docker command: `docker compose up -d --build` inside `/root/notes`
- Git branch deploy, NOT merge to main yet — wait for user confirmation
  </action>
  <verify>
    <automated>ssh root@192.168.1.50 "cd /root/notes && docker compose ps" 2>&1 | grep -E "running|Up"</automated>
  </verify>
  <done>All tests green. Docker container running. App accessible at the production URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of all Phase 2 features</name>
  <what-built>
Complete Phase 2 Core Outliner:
- Bullet tree rendering from server data
- Enter/Tab/Shift+Tab/Backspace/Delete keyboard editing per CONTEXT.md spec
- Drag-and-drop reorder with cross-level reparenting (dot-as-handle, 5px threshold)
- Single-click bullet dot = zoom into that bullet (URL changes, breadcrumb appears)
- Breadcrumb navigation: click any ancestor to zoom to that level
- Ctrl+Z / Ctrl+Y undo/redo with server-side persistence (survives page refresh)
- Collapse/expand chevron for bullets with children (persisted to server)
- Complete bullets (strikethrough + 50% opacity); hide/show completed toggle
- Right-click context menu with all bullet actions
- Bulk delete completed bullets (with confirmation)
- All keyboard shortcuts: Enter, Tab, Shift+Tab, Ctrl+Arrow, Ctrl+Z/Y, Ctrl+]/[, Ctrl+E
  </what-built>
  <how-to-verify>
Visit https://notes.gregorymaingret.fr and run these checks:

**Basic editing:**
1. Open a document. Click into the bullet area and start typing — text should appear.
2. Press Enter — a new bullet should appear below with cursor focused.
3. Type text, press Tab — bullet indents (becomes child). Press Shift+Tab — outdents.
4. Press Backspace at start of a bullet with text — merges with bullet above.
5. Press Backspace at start of a bullet WITH CHILDREN — a shake animation appears, NO merge.

**Undo:**
6. Make a change (e.g., indent a bullet). Press Ctrl+Z — change should reverse.
7. Refresh the page. Press Ctrl+Z again — undo history should still work (server-side).

**Drag-and-drop:**
8. Grab a bullet by its dot (•) and drag it. A horizontal line should show the drop position. Release — bullet moves to new position.
9. Single click on bullet dot (no drag) — URL should change to #bullet/:id and the bullet becomes the zoomed root.

**Zoom + breadcrumb:**
10. After zooming in, breadcrumb should appear at the top instead of the document title.
11. Click document title in breadcrumb — returns to doc root view.

**Collapse:**
12. Create child bullets. Click the ▾ chevron — children collapse (▶ shows). Refresh page — collapsed state survives.

**Complete:**
13. Right-click a bullet — context menu opens. Click "Mark complete" — bullet gets strikethrough + fades. Right-click → "Unmark complete" reverts.
14. Click "Hide completed" in toolbar — completed bullets disappear. Toggle again — they reappear.

**Delete completed:**
15. Mark a few bullets complete. Click "Delete completed" — confirmation dialog appears. Confirm — completed bullets permanently removed.

**iOS Safari (if available — required before phase sign-off per STATE.md blocker):**
16. On iOS Safari, press Enter in a bullet — new bullet should be created and focused immediately in the same gesture.
  </how-to-verify>
  <files></files>
  <action>Human verification step — no automated action. Review the how-to-verify checklist above at https://notes.gregorymaingret.fr.</action>
  <verify>
    <automated>MISSING — human checkpoint; verification is manual per how-to-verify checklist</automated>
  </verify>
  <done>User types "approved" after completing all 16 verification steps.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human checkpoint approved. All Phase 2 requirements verified in production.
</verification>

<success_criteria>
- Full vitest suite green (server + client)
- Production deployment successful
- All 24 Phase 2 requirements verified by user at https://notes.gregorymaingret.fr
- Undo history confirmed to survive page refresh
- iOS Safari Enter behavior confirmed (STATE.md blocker cleared)
- PR created, CI passes, merged to main
- Server updated to main branch after merge
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-outliner/02-08-SUMMARY.md`
</output>
