---
phase: 03-rich-content
plan: 09
type: execute
wave: 5
depends_on:
  - 03-08
files_modified: []
autonomous: false

requirements:
  - BULL-09
  - BULL-10
  - TAG-01
  - TAG-02
  - TAG-03
  - TAG-04
  - TAG-05
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - BM-01
  - BM-02
  - BM-03

must_haves:
  truths:
    - "Bold/italic/strikethrough/links render in bullets when not editing"
    - "#tag @mention !!date chips are clickable in rendered bullets"
    - "!! while typing opens native date picker and inserts chip"
    - "Sidebar Tags tab shows grouped tags with filter input"
    - "Tag/mention click in Tag Browser shows filtered bullet list"
    - "Ctrl+F opens centered search modal; results highlight matched text"
    - "Clicking a search result opens that bullet in zoomed focus view"
    - "Bookmarks toolbar button shows bookmarks canvas"
    - "Clicking a bookmark opens that bullet in zoomed focus view"
    - "All automated tests pass (client + server)"
  artifacts:
    - path: "https://notes.gregorymaingret.fr"
      provides: "Running production build with all Phase 3 features"
  key_links: []

user_setup: []
---

<objective>
Wave 5: Deploy to production and run human verification checkpoint. All Phase 3 features are verified on the live server at https://notes.gregorymaingret.fr before merging to main.

Purpose: Per CLAUDE.md workflow — deploy and get user confirmation before merging the phase branch.
Output: Phase branch deployed, user approval received, PR merged to main.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rich-content/03-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all tests, push branch, deploy to server</name>
  <files></files>
  <action>
    Follow the CLAUDE.md Standard Workflow exactly:

    1. Run server tests on the Docker host:
    `ssh root@192.168.1.50 "cd /root/notes/server && npx vitest run --reporter=dot"`

    2. Run Flutter (client) tests locally:
    `cd /c/Users/gmain/Dev/Notes/client && npx vitest run`

    3. Run TypeScript check locally:
    `cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit`
    `cd /c/Users/gmain/Dev/Notes/server && npx tsc --noEmit`

    4. Push the phase branch (do NOT merge yet):
    `git push origin phase-3/rich-content`

    5. Deploy to server:
    `ssh root@192.168.1.50 "cd /root/notes && git fetch && git checkout phase-3/rich-content && docker compose up -d --build"`

    6. Wait for Docker build to complete. Verify app is running:
    `ssh root@192.168.1.50 "cd /root/notes && docker compose logs app --tail=20"`

    Do NOT merge to main yet — wait for user confirmation.
  </action>
  <verify>
    <automated>ssh root@192.168.1.50 "cd /root/notes && docker compose logs app --tail=5 2>&1 | grep 'Server running'"</automated>
  </verify>
  <done>All tests pass; branch pushed; Docker container running with Phase 3 code</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 3 Rich Content features deployed to https://notes.gregorymaingret.fr:
    - Inline markdown rendering (bold, italic, strikethrough, links) in bullets
    - Chip syntax: #tag, @mention, !![date] render as clickable chips
    - !! while typing opens native date picker
    - Sidebar [Docs][Tags] tab bar; Tags tab shows grouped tag browser with filter
    - Clicking a tag opens filtered bullet list canvas
    - Ctrl+F / search icon opens centered search modal with highlighting
    - Bookmarks: toolbar icon + Ctrl+* opens bookmarks canvas; right-click → Bookmark
  </what-built>
  <how-to-verify>
    1. Open https://notes.gregorymaingret.fr and log in

    2. Markdown rendering (BULL-09, BULL-10):
       - Create a bullet with `**bold** and *italic* and ~~strike~~`
       - Click away — verify it renders formatted
       - Click the bullet — verify raw markdown shows

    3. Chips (TAG-01, TAG-02, TAG-03):
       - Create a bullet with `buy #milk from @alice`
       - Click away — verify #milk and @alice render as colored chips
       - Type `!!` in a bullet — verify native date picker opens, date inserts as chip

    4. Tag Browser (TAG-04, TAG-05):
       - Click the Tags tab in the sidebar
       - Verify #milk and @alice appear under their respective groups with counts
       - Type in the filter input — verify list narrows
       - Click #milk — verify canvas shows filtered bullet list with "• buy #milk from @alice [DocumentName]"
       - Click the matching bullet row — verify it zooms to that bullet

    5. Search (SRCH-01 to SRCH-04):
       - Press Ctrl+F — verify centered search modal opens (browser find bar must NOT open)
       - Type "milk" — verify results appear with "milk" highlighted
       - Click a result — verify navigation to that bullet in zoomed focus

    6. Bookmarks (BM-01 to BM-03):
       - Right-click a bullet → select "Bookmark" from context menu
       - Click the Bookmarks button in the toolbar (or press Ctrl+*)
       - Verify the bookmarked bullet appears in the bookmarks canvas
       - Click the ★ icon on the row — verify it unbookmarks
       - Click the bullet row — verify it navigates to that bullet
  </how-to-verify>
  <resume-signal>Type "approved" to merge to main, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge phase branch to main (after approval)</name>
  <files></files>
  <action>
    After user approves:

    1. Create a PR and merge:
    `gh pr create --title "Phase 3: Rich Content" --body "Adds inline markdown rendering, chip syntax (#tag @mention !!date), Tag Browser, full-text search modal, and bookmarks. Closes all BULL-09, BULL-10, TAG-01 through TAG-05, SRCH-01 through SRCH-04, BM-01 through BM-03 requirements." --base main --head phase-3/rich-content`

    2. Merge the PR (after CI passes): `gh pr merge --squash`

    3. Update Docker host to main (no rebuild needed per MEMORY.md — image already has the code):
    `ssh root@192.168.1.50 "cd /root/notes && git checkout main && git pull origin main"`

    4. Update .planning/ROADMAP.md: mark Phase 3 as Complete with today's date.
  </action>
  <verify>
    <automated>ssh root@192.168.1.50 "cd /root/notes && git branch --show-current"</automated>
  </verify>
  <done>Phase branch merged to main; Docker host on main; ROADMAP.md updated</done>
</task>

</tasks>

<verification>
- `cd client && npx vitest run` — all pass
- `cd server && npx vitest run` — all pass
- `ssh root@192.168.1.50 "cd /root/notes && git branch --show-current"` → main
- User has confirmed all 14 Phase 3 requirements work on production
</verification>

<success_criteria>
- All automated tests green (client + server)
- User confirmed all Phase 3 features work on https://notes.gregorymaingret.fr
- phase-3/rich-content merged to main
- ROADMAP.md shows Phase 3 as Complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-rich-content/03-09-SUMMARY.md`
</output>
