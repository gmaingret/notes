---
phase: 04-attachments-comments-and-mobile
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/attachments.test.ts
  - server/tests/bullets.test.ts
  - client/src/test/attachmentRow.test.tsx
  - client/src/test/pdfThumbnail.test.ts
  - client/src/test/noteRow.test.tsx
  - client/src/test/swipeGesture.test.ts
  - client/src/test/longPress.test.ts
  - client/src/test/keyboardOffset.test.ts
autonomous: true
requirements:
  - ATT-01
  - ATT-02
  - ATT-03
  - ATT-04
  - ATT-05
  - ATT-06
  - CMT-01
  - CMT-02
  - CMT-03
  - CMT-04
  - MOB-01
  - MOB-02
  - MOB-03
  - BULL-16
  - MOB-05

must_haves:
  truths:
    - "All 8 test files exist and fail with 'not implemented' (RED state)"
    - "Server attachment tests cover upload, list, delete, and disk path"
    - "Client tests cover image/PDF/other rendering, note display and clear, swipe threshold logic, long-press timer logic, and visualViewport offset calculation"
    - "Both vitest suites run without framework errors — only test assertion failures"
  artifacts:
    - path: "server/tests/attachments.test.ts"
      provides: "RED stubs for ATT-01, ATT-02, ATT-06"
    - path: "server/tests/bullets.test.ts"
      provides: "Extended with RED stub for CMT-03 (note patch)"
    - path: "client/src/test/attachmentRow.test.tsx"
      provides: "RED stubs for ATT-03, ATT-05"
    - path: "client/src/test/pdfThumbnail.test.ts"
      provides: "RED stub for ATT-04 (pdfjsLib mocked)"
    - path: "client/src/test/noteRow.test.tsx"
      provides: "RED stubs for CMT-01, CMT-02, CMT-04"
    - path: "client/src/test/swipeGesture.test.ts"
      provides: "RED stubs for MOB-01, MOB-02"
    - path: "client/src/test/longPress.test.ts"
      provides: "RED stubs for BULL-16, MOB-03"
    - path: "client/src/test/keyboardOffset.test.ts"
      provides: "RED stub for MOB-05"
  key_links:
    - from: "client/src/test/attachmentRow.test.tsx"
      to: "client/src/components/DocumentView/AttachmentRow.tsx"
      via: "import (not yet created)"
      pattern: "import.*AttachmentRow"
    - from: "client/src/test/noteRow.test.tsx"
      to: "client/src/components/DocumentView/NoteRow.tsx"
      via: "import (not yet created)"
      pattern: "import.*NoteRow"
---

<objective>
Create failing Nyquist test scaffolds (RED state) for all Phase 4 requirements before any implementation begins. These stubs define the test contracts that later plans must make pass.

Purpose: Nyquist compliance — every requirement has an automated feedback signal from the first commit, and no implementation can silently skip a requirement.
Output: 8 test files (2 server, 6 client), all failing with assertion errors (not import errors).
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-attachments-comments-and-mobile/04-CONTEXT.md
@.planning/phases/04-attachments-comments-and-mobile/04-RESEARCH.md
@.planning/phases/04-attachments-comments-and-mobile/04-VALIDATION.md
</context>

<interfaces>
<!-- Existing test patterns from prior phases — follow exactly -->

From server/tests/bullets.test.ts (pattern):
```typescript
import { describe, it, expect } from 'vitest';
// Tests import services and assert behavior
// RED stubs: expect(true).toBe(false) or throw new Error('not implemented')
```

From client/src/test/setup.ts and existing tests (pattern):
```typescript
import { describe, it, expect, vi } from 'vitest';
// Client tests use vi.mock() for external dependencies
// Pure logic tests — no React mounting for gesture/timer logic
```

From client/src/test/bulletContent.test.ts (existing, pattern):
```typescript
// Pure function tests — import logic helpers, not full React components
// For component tests: import { render } from '@testing-library/react'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server test stubs — attachments service + note patch</name>
  <files>server/tests/attachments.test.ts, server/tests/bullets.test.ts</files>
  <behavior>
    - ATT-01: uploadAttachment(userId, bulletId, file) returns object with {id, filename, mimeType, size, bulletId}
    - ATT-06: uploadAttachment stores file to /data/attachments path (storagePath starts with /data/attachments)
    - ATT-02 (server): getAttachmentsByBullet(userId, bulletId) returns array of attachments
    - deleteAttachment(userId, attachmentId) removes record and returns {deleted: true}
    - CMT-03: patchBullet with {note: 'some text'} persists note; patchBullet with {note: ''} stores null in DB
  </behavior>
  <action>
    Create server/tests/attachments.test.ts as a new file. Import describe/it/expect from 'vitest'. Write 4 test cases:
    (1) "uploads attachment and returns record" — calls a not-yet-existing uploadAttachment function, expect(true).toBe(false) as stub,
    (2) "storagePath is under /data/attachments" — same stub pattern,
    (3) "lists attachments by bullet" — stub,
    (4) "deletes attachment removes record" — stub.
    All stubs use `throw new Error('not implemented — implement in 04-03')` so they fail as RED.

    Extend server/tests/bullets.test.ts: add a describe block "note patch" with 2 tests:
    (1) "PATCH with note text persists note" — throw new Error('not implemented — implement in 04-02'),
    (2) "PATCH with empty note stores null" — throw new Error('not implemented — implement in 04-02').

    Do NOT import non-existent modules at the top level — import inside the test body using dynamic import or just use the throw-stub pattern to keep test runnable.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/server && npx vitest run tests/attachments.test.ts tests/bullets.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Tests run (not import-error crash), all new stubs fail with "not implemented" error messages. Existing bullets tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client test stubs — attachment rendering, note row, PDF thumbnail</name>
  <files>client/src/test/attachmentRow.test.tsx, client/src/test/pdfThumbnail.test.ts, client/src/test/noteRow.test.tsx</files>
  <behavior>
    - ATT-03: Image attachment row renders img element (via object URL) — stub
    - ATT-05: Non-image/PDF attachment shows filename text + download trigger — stub
    - ATT-04: renderPdfThumbnail(blob, canvas) calls pdfjs getDocument — stub (mock pdfjs-dist)
    - CMT-01: BulletNode with non-empty note renders NoteRow below BulletContent — stub
    - CMT-02: Clicking note icon in focus toolbar focuses NoteRow contenteditable — stub
    - CMT-04: Clearing NoteRow text triggers PATCH with note=null — stub
  </behavior>
  <action>
    Create client/src/test/attachmentRow.test.tsx: import describe/it/expect from 'vitest'. Write 2 describe blocks:
    "image attachment" with test "renders img with object URL" (throw new Error('not implemented — implement in 04-04')),
    "other file attachment" with test "shows filename and download link" (same stub pattern).

    Create client/src/test/pdfThumbnail.test.ts: vi.mock('pdfjs-dist', ...) at top. Write test "renders first page to canvas" (throw new Error('not implemented — implement in 04-04')).

    Create client/src/test/noteRow.test.tsx: Write 3 tests:
    "NoteRow renders when bullet has note" — stub,
    "NoteRow hidden when bullet note is null" — stub,
    "clearing note text triggers patch with null" — stub.

    All stub with throw new Error('not implemented — implement in 04-04') or 04-02 as appropriate.
    Keep imports minimal — use dynamic imports inside test body if the component doesn't exist yet to avoid module-not-found crashes.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/attachmentRow.test.tsx src/test/pdfThumbnail.test.ts src/test/noteRow.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>3 files exist, all tests fail with "not implemented" errors (not import crashes). Vitest exits with failure code but no framework errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Client test stubs — gesture logic (swipe, long-press, keyboard offset)</name>
  <files>client/src/test/swipeGesture.test.ts, client/src/test/longPress.test.ts, client/src/test/keyboardOffset.test.ts</files>
  <behavior>
    - MOB-01: swipeThresholdReached(swipeX, rowWidth, direction='right') returns true when swipeX > rowWidth*0.4
    - MOB-02: swipeThresholdReached(swipeX, rowWidth, direction='left') returns true when swipeX < -(rowWidth*0.4)
    - swipeThresholdReached returns false when below threshold (both directions)
    - BULL-16 / MOB-03: createLongPressHandler({onLongPress, delay=500}) returns {handleTouchStart, handleTouchMove, handleTouchEnd}
    - Long press fires after 500ms if no touchmove > 8px
    - touchmove > 8px cancels the timer (onLongPress not called)
    - MOB-05: computeKeyboardOffset(windowInnerHeight, vvOffsetTop, vvHeight) returns correct bottom offset
    - Returns 0 when keyboard is not open (vvHeight approaches windowInnerHeight)
  </behavior>
  <action>
    These tests are pure logic tests — no DOM or React needed, just import the helper functions that will be created in later plans.

    Create client/src/test/swipeGesture.test.ts: Write tests for a pure function swipeThresholdReached(swipeX: number, rowWidth: number): 'complete' | 'delete' | null.
    Test: swipeX=130, rowWidth=300 → 'complete' (130 > 120). Test: swipeX=-130, rowWidth=300 → 'delete'. Test: swipeX=50, rowWidth=300 → null.
    Stub: import { swipeThresholdReached } from '../components/DocumentView/BulletNode' — this module doesn't exist yet, use a try/catch wrapper or define the stubs to throw. The cleanest approach: define a local placeholder `const swipeThresholdReached = (): never => { throw new Error('not implemented') }` at top and write real expectations that will pass once replaced with real import.

    Create client/src/test/longPress.test.ts: Write 2 tests using fake timers (vi.useFakeTimers()):
    "fires onLongPress after 500ms" — use vi.advanceTimersByTime(500), expect(callback).toHaveBeenCalled().
    "cancels on touchmove > 8px" — advanceTimersByTime(300), trigger move, advance to 500, expect not called.
    Stub: define createLongPressHandler as a local function that throws 'not implemented'.

    Create client/src/test/keyboardOffset.test.ts: Write 2 tests for computeKeyboardOffset(windowInnerHeight: number, vvOffsetTop: number, vvHeight: number): number.
    "returns offset when keyboard open": computeKeyboardOffset(844, 0, 500) → 344. "returns 0 when keyboard closed": computeKeyboardOffset(844, 0, 844) → 0.
    Stub: local placeholder function throws 'not implemented'.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/swipeGesture.test.ts src/test/longPress.test.ts src/test/keyboardOffset.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>3 files exist, all tests fail (RED) — stubs throw "not implemented". No framework import errors. Pure logic test pattern matches existing test suite style.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `cd /c/Users/gmain/Dev/Notes/server && npx vitest run --reporter=verbose` — existing tests pass, new stubs fail
- `cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose` — existing tests pass, new stubs fail
- Total new test files: 8 (2 server, 6 client)
- All Phase 4 requirement IDs are represented by at least one test stub
</verification>

<success_criteria>
- 8 test files committed on branch phase-4/attachments-comments-mobile
- `npx vitest run` on both server and client exits with FAIL (not crash)
- Existing passing tests remain green
- Each stub's error message names the plan that will implement it (e.g., "implement in 04-02")
</success_criteria>

<output>
After completion, create `.planning/phases/04-attachments-comments-and-mobile/04-01-SUMMARY.md`
</output>
