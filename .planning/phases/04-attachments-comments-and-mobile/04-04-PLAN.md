---
phase: 04-attachments-comments-and-mobile
plan: "04"
type: execute
wave: 2
depends_on: ["04-02", "04-03"]
files_modified:
  - client/src/hooks/useAttachments.ts
  - client/src/components/DocumentView/NoteRow.tsx
  - client/src/components/DocumentView/AttachmentRow.tsx
  - client/src/components/DocumentView/Lightbox.tsx
  - client/src/components/DocumentView/BulletNode.tsx
  - client/src/hooks/useBullets.ts
  - client/src/test/noteRow.test.tsx
  - client/src/test/attachmentRow.test.tsx
  - client/src/test/pdfThumbnail.test.ts
autonomous: true
requirements:
  - ATT-01
  - ATT-02
  - ATT-03
  - ATT-04
  - ATT-05
  - CMT-01
  - CMT-02
  - CMT-03
  - CMT-04

must_haves:
  truths:
    - "NoteRow renders below BulletContent when bullet.note is non-empty; hidden when null"
    - "NoteRow is a contenteditable div; editing saves on blur via PATCH; Escape blurs without saving (stopPropagation)"
    - "Clearing NoteRow text PATCHes with note=null"
    - "AttachmentRow renders one row per attachment below NoteRow"
    - "Image attachment row shows ~80px thumbnail fetched as blob (Object URL); clicking opens Lightbox"
    - "PDF attachment row shows canvas thumbnail via pdfjs-dist; clicking opens PDF in new tab"
    - "Other file attachment row shows filename + download icon; clicking downloads file"
    - "Lightbox shows full-resolution image centered; Esc or click-outside closes it"
    - "useAttachments hook provides useBulletAttachments and useUploadAttachment with React Query"
  artifacts:
    - path: "client/src/hooks/useAttachments.ts"
      provides: "useBulletAttachments, useUploadAttachment, useDeleteAttachment"
      exports: ["useBulletAttachments", "useUploadAttachment", "useDeleteAttachment"]
    - path: "client/src/components/DocumentView/NoteRow.tsx"
      provides: "Inline contenteditable note row below bullet text"
      min_lines: 40
    - path: "client/src/components/DocumentView/AttachmentRow.tsx"
      provides: "Single attachment row for image/PDF/other with type-specific rendering"
      min_lines: 60
    - path: "client/src/components/DocumentView/Lightbox.tsx"
      provides: "Full-screen image overlay with Esc/click-outside close"
      min_lines: 25
    - path: "client/src/components/DocumentView/BulletNode.tsx"
      provides: "NoteRow + AttachmentRows rendered below BulletContent"
  key_links:
    - from: "client/src/components/DocumentView/BulletNode.tsx"
      to: "client/src/components/DocumentView/NoteRow.tsx"
      via: "conditional render when bullet.note !== null"
      pattern: "NoteRow"
    - from: "client/src/components/DocumentView/AttachmentRow.tsx"
      to: "client/src/hooks/useAttachments.ts"
      via: "useDeleteAttachment mutation"
      pattern: "useDeleteAttachment"
    - from: "client/src/components/DocumentView/AttachmentRow.tsx"
      to: "/api/attachments/:id/file"
      via: "apiClient.download() → URL.createObjectURL"
      pattern: "download.*attachment"
---

<objective>
Build the inline bullet augmentation UI: NoteRow (Dynalist-style inline note below each bullet), AttachmentRow (per-attachment display row with image/PDF/other rendering), Lightbox overlay, and wire these into BulletNode. Also creates the useAttachments React Query hook.

Purpose: This is the user-visible output of the attachments and notes features. After this plan, users can see notes inline and view attachment previews.
Output: 5 new components/hooks + BulletNode and useBullets modifications.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attachments-comments-and-mobile/04-CONTEXT.md
@.planning/phases/04-attachments-comments-and-mobile/04-RESEARCH.md
@.planning/phases/04-attachments-comments-and-mobile/04-01-SUMMARY.md
@.planning/phases/04-attachments-comments-and-mobile/04-02-SUMMARY.md
@.planning/phases/04-attachments-comments-and-mobile/04-03-SUMMARY.md
</context>

<interfaces>
<!-- Install pdfjs-dist before implementing AttachmentRow -->
<!-- client: npm install pdfjs-dist -->

From client/src/hooks/useBookmarks.ts (pattern for useAttachments):
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { apiClient } from '../api/client';
// queryKey pattern: ['attachments', bulletId]
// onSettled: qc.invalidateQueries({ queryKey: ['attachments', bulletId] })
```

From client/src/components/DocumentView/BulletNode.tsx (current render structure):
```typescript
// Current: <div ref={setNodeRef} style={style} {...attributes}>
//   <div class="dot"> ... </div>
//   <BulletContent bullet={bullet} ... />
//   {contextMenuPos && <ContextMenu ... />}
// </div>
// After this plan: add <NoteRow> and <AttachmentRow[]> after BulletContent
```

From client/src/hooks/useBullets.ts (Bullet type to extend):
```typescript
// Current Bullet type includes: id, content, isComplete, isCollapsed, deletedAt, ...
// Add: note: string | null
```

Attachment type (from attachmentService):
```typescript
type Attachment = {
  id: string;
  bulletId: string;
  userId: string;
  filename: string;
  mimeType: string;
  size: number;
  storagePath: string;
  createdAt: string;
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useAttachments hook + NoteRow component</name>
  <files>client/src/hooks/useAttachments.ts, client/src/components/DocumentView/NoteRow.tsx, client/src/hooks/useBullets.ts, client/src/test/noteRow.test.tsx</files>
  <behavior>
    - useBulletAttachments(bulletId): useQuery fetching GET /api/attachments/bullets/:bulletId
    - useUploadAttachment(): useMutation posting FormData to /api/attachments/bullets/:bulletId via apiClient.upload()
    - useDeleteAttachment(): useMutation DELETE /api/attachments/:id; invalidates ['attachments', bulletId]
    - Bullet type in useBullets.ts gains note: string | null field
    - NoteRow renders a contenteditable div when bullet.note !== null OR when noteVisible state is true (triggered by focus toolbar)
    - NoteRow onBlur: if textContent equals original note → no-op; if changed → call usePatchNote mutation (add usePatchNote to useBullets.ts: PATCH /api/bullets/:id with { note: text || null })
    - NoteRow onKeyDown: Escape → e.stopPropagation() + blur (does NOT trigger save — Esc means cancel, revert to original note text)
    - NoteRow when empty/cleared → triggers PATCH with note=null on blur
    - noteRow.test.tsx stubs updated to real tests (no more throw stubs)
  </behavior>
  <action>
    Run: `cd /c/Users/gmain/Dev/Notes/client && npm install pdfjs-dist`

    Create client/src/hooks/useAttachments.ts following useBookmarks.ts pattern. Export:
    - `Attachment` type
    - `useBulletAttachments(bulletId: string)`
    - `useUploadAttachment()` — mutationFn takes {bulletId, file: File}, creates FormData, calls apiClient.upload()
    - `useDeleteAttachment()` — mutationFn takes {attachmentId, bulletId}, calls apiClient.delete<void>('/api/attachments/'+attachmentId), invalidates ['attachments', bulletId]

    In client/src/hooks/useBullets.ts: add `note: string | null` to the Bullet type export. Add `usePatchNote()` mutation that calls apiClient.patch('/api/bullets/'+id, { note }).

    Create client/src/components/DocumentView/NoteRow.tsx:
    - Props: { bulletId: string; initialNote: string | null; focusOnMount?: boolean }
    - Uses usePatchNote mutation
    - Renders: `<div contentEditable suppressContentEditableWarning style={{ fontSize: '0.85em', color: '#888', marginLeft: 24, minHeight: '1em' }} ... />`
    - useLayoutEffect: set textContent to initialNote, call .focus() if focusOnMount
    - onBlur: compare current textContent to initialNote; if changed, mutate({ id: bulletId, note: textContent || null })
    - onKeyDown: Escape → e.stopPropagation(); revert textContent to initialNote; (ref).blur()
    - The Escape key stopPropagation prevents the global keyboard handler from firing (Pitfall 7 from RESEARCH.md)

    Update client/src/test/noteRow.test.tsx to replace throw stubs with real assertions using @testing-library/react render or pure logic tests.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/noteRow.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>noteRow.test.tsx all tests pass. NoteRow.tsx and useAttachments.ts exist. Bullet type has note field. TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AttachmentRow + Lightbox + wire into BulletNode</name>
  <files>client/src/components/DocumentView/AttachmentRow.tsx, client/src/components/DocumentView/Lightbox.tsx, client/src/components/DocumentView/BulletNode.tsx, client/src/test/attachmentRow.test.tsx, client/src/test/pdfThumbnail.test.ts</files>
  <behavior>
    - AttachmentRow receives: attachment: Attachment; onDelete: () => void
    - Image detection: attachment.mimeType.startsWith('image/')
    - PDF detection: attachment.mimeType === 'application/pdf'
    - Image row: useEffect fetches blob via apiClient.download('/api/attachments/'+id+'/file'), creates Object URL, sets imgSrc state; cleanup revokes Object URL
    - Image thumbnail: <img src={imgSrc} style={{ height: 80, objectFit: 'cover', cursor: 'pointer' }} onClick → setLightboxOpen(true) />
    - Lightbox: position:fixed overlay, centered <img>, Esc key closes, click outside image closes
    - PDF row: <canvas ref> useEffect calls renderPdfThumbnail (see below); onClick → window.open('/api/attachments/'+id+'/file') — wait, PDF needs auth too, so use apiClient.download() to fetch blob → URL.createObjectURL → window.open(objectUrl)
    - Other files: shows 📎 + filename text; onClick → apiClient.download() fetch → response.blob() → create Object URL → create <a download> element → click → revoke
    - Delete [x] button on each row calls onDelete
    - renderPdfThumbnail(blob: Blob, canvas: HTMLCanvasElement): uses pdfjs-dist, sets GlobalWorkerOptions.workerSrc to `https://unpkg.com/pdfjs-dist@${pdfjsLib.version}/build/pdf.worker.min.mjs` (check actual .mjs vs .js after install), renders page 1 at scale 0.3

    BulletNode wiring:
    - Import useBulletAttachments, useDeleteAttachment from useAttachments
    - const { data: attachments = [] } = useBulletAttachments(bullet.id)
    - Render after BulletContent: {bullet.note !== null && <NoteRow bulletId={bullet.id} initialNote={bullet.note} />}
    - Then: {attachments.map(a => <AttachmentRow key={a.id} attachment={a} onDelete={() => deleteAttachment.mutate({attachmentId: a.id, bulletId: bullet.id})} />)}
  </behavior>
  <action>
    Create client/src/components/DocumentView/Lightbox.tsx:
    Simple custom overlay (~35 lines). Props: { src: string; onClose: () => void }. Renders:
    - `<div style={{ position:'fixed', inset:0, background:'rgba(0,0,0,0.85)', zIndex:3000, display:'flex', alignItems:'center', justifyContent:'center' }} onClick={onClose}>`
    - Inside: `<img src={src} style={{ maxWidth:'90vw', maxHeight:'90vh', objectFit:'contain' }} onClick={e => e.stopPropagation()} />`
    - useEffect: add keydown listener for Escape → onClose; cleanup on unmount

    Create client/src/components/DocumentView/AttachmentRow.tsx implementing the behavior above. Export renderPdfThumbnail as a named export for testing.

    Modify client/src/components/DocumentView/BulletNode.tsx: add NoteRow + AttachmentRow rendering below BulletContent as described. Add delete mutation wiring. Import useBulletAttachments.

    Update test stubs:
    - attachmentRow.test.tsx: Replace throw stubs with tests that mock apiClient.download() (vi.mock) and verify img renders with object URL, and other-file row shows filename.
    - pdfThumbnail.test.ts: Replace throw stub with test that mocks pdfjs-dist (vi.mock('pdfjs-dist', ...)) and verifies renderPdfThumbnail calls getDocument.

    Check actual pdfjs-dist worker filename after install:
    `ls /c/Users/gmain/Dev/Notes/client/node_modules/pdfjs-dist/build/ | grep worker`
    Use the exact filename (.mjs or .js) in the workerSrc URL.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/attachmentRow.test.tsx src/test/pdfThumbnail.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files pass. AttachmentRow.tsx, Lightbox.tsx exist. BulletNode.tsx renders NoteRow and AttachmentRows. TypeScript clean.</done>
</task>

</tasks>

<verification>
- `cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit` — clean
- `cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/noteRow.test.tsx src/test/attachmentRow.test.tsx src/test/pdfThumbnail.test.ts` — all pass
- BulletNode.tsx imports NoteRow and AttachmentRow
- Lightbox.tsx is self-contained (no external lightbox library)
- pdfjs-dist installed in client/package.json
</verification>

<success_criteria>
- Note appears inline below bullet when bullet.note is non-empty
- Escape in note reverts and stops propagation (no global handler interference)
- Image attachment shows ~80px thumbnail via Object URL; click opens Lightbox
- PDF attachment shows canvas thumbnail via pdfjs-dist; click opens PDF in new tab
- Other files show filename + click downloads
- Delete [x] button removes attachment row
- All 3 test files green
</success_criteria>

<output>
After completion, create `.planning/phases/04-attachments-comments-and-mobile/04-04-SUMMARY.md`
</output>
