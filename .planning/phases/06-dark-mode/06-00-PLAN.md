---
phase: 06-dark-mode
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - client/src/test/darkMode.test.tsx
autonomous: true
requirements:
  - DRKM-03
  - DRKM-04

must_haves:
  truths:
    - "darkMode.test.tsx exists with failing tests for FOUC script behavior and meta tag presence"
    - "Tests are RED before any implementation (Wave 0 contract)"
  artifacts:
    - path: "client/src/test/darkMode.test.tsx"
      provides: "Automated test coverage for DRKM-03 and DRKM-04"
      exports: []
  key_links:
    - from: "client/src/test/darkMode.test.tsx"
      to: "client/index.html"
      via: "file read assertion for meta tag"
      pattern: "color-scheme.*content.*light dark"
    - from: "client/src/test/darkMode.test.tsx"
      to: "window.matchMedia mock"
      via: "vi.fn() stub"
      pattern: "matchMedia.*dark.*classList.*add.*dark"
---

<objective>
Write failing tests for DRKM-03 (FOUC prevention script) and DRKM-04 (color-scheme meta tag) before any implementation exists. These tests become GREEN in Wave 3 after the FOUC script and meta tag are added to index.html.

Purpose: Establish the Nyquist-compliant automated gate for the two behavioral requirements that CAN be unit tested (FOUC script and meta tag presence). DRKM-01 and DRKM-02 are verified by file content assertion and visual inspection respectively.

Output: client/src/test/darkMode.test.tsx — RED at time of commit.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dark-mode/06-CONTEXT.md
@.planning/phases/06-dark-mode/06-RESEARCH.md
</context>

<feature>
  <name>Dark Mode Automated Tests</name>
  <files>client/src/test/darkMode.test.tsx</files>
  <behavior>
    Test 1 — DRKM-04 meta tag: Read the raw text of client/index.html and assert it contains `name="color-scheme"` and `content="light dark"`. This fails until the meta tag is added in Wave 3.

    Test 2 — DRKM-03 FOUC class added: Mock `window.matchMedia` to return `{ matches: true }` for `(prefers-color-scheme: dark)`. Read the raw text of client/index.html and extract the inline script content. Eval the script in jsdom context with the mock in place. Assert `document.documentElement.classList.contains('dark')` is true. This fails until the inline FOUC script is added in Wave 3.

    Test 3 — DRKM-03 FOUC class not added in light mode: Mock `window.matchMedia` to return `{ matches: false }`. Run same eval. Assert classList does NOT contain 'dark'.

    Test 4 — DRKM-01 file assertion: Read client/src/index.css raw text and assert it contains `@media (prefers-color-scheme: dark)` block and `--color-bg-base` token. This fails until Wave 1 adds the token block.
  </behavior>
  <implementation>
    Use `fs.readFileSync` to read `client/index.html` and `client/src/index.css` relative to the project root (use `path.resolve(__dirname, '../../../index.html')` from `src/test/` — adjust for the Vitest `__dirname` equivalent using `import.meta.url`).

    For the FOUC script test: extract the `<script>` block content from index.html, then call it as a function with the mocked `window.matchMedia` — simplest approach is to wrap the extracted script content in a `Function()` constructor and call it, or just run the assertions on the presence of the script tag and its expected code pattern (string contains check is sufficient for a behavioral contract).

    Actually, simplest and most robust: use string assertions only. Test that index.html CONTAINS the exact FOUC script pattern string `document.documentElement.classList.add('dark')`. No eval needed. The test verifies the correct code is present in the file, which is the implementation contract.

    Pattern for resolving paths in Vitest: `new URL('../../../index.html', import.meta.url).pathname` — this works on all platforms.

    Import: `import { readFileSync } from 'fs'`, `import { fileURLToPath } from 'url'`, `import { describe, it, expect } from 'vitest'`
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write darkMode.test.tsx (RED state)</name>
  <files>client/src/test/darkMode.test.tsx</files>
  <behavior>
    - Test: index.html contains `name="color-scheme"` and `content="light dark"` (DRKM-04)
    - Test: index.html contains the FOUC script pattern `document.documentElement.classList.add('dark')` (DRKM-03)
    - Test: index.html contains `window.matchMedia('(prefers-color-scheme: dark)').matches` (DRKM-03 mechanism)
    - Test: index.css contains `@media (prefers-color-scheme: dark)` (DRKM-01 file assertion)
    - Test: index.css contains `--color-bg-base` (DRKM-01 token existence)
  </behavior>
  <action>
    Create client/src/test/darkMode.test.tsx. Read files using `readFileSync`. Resolve paths using `fileURLToPath(new URL(..., import.meta.url))` pattern — navigate three levels up from src/test/ to reach the client/ root, then one more to reach project root for index.html.

    Path structure: test file is at `client/src/test/darkMode.test.tsx`. index.html is at `client/index.html` (two dirs up from `src/test/`). index.css is at `client/src/index.css` (one dir up from `src/test/`).

    Write five `it()` blocks inside a `describe('Dark Mode')` block. All five must FAIL (RED) at commit time because index.html currently has no meta tag, no FOUC script, and index.css has no token block.

    After writing, run `cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/darkMode.test.tsx` and confirm ALL tests fail. If any pass unexpectedly, investigate — the implementation may already exist.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/darkMode.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>darkMode.test.tsx exists. Running the test suite shows all 5 tests FAILED (red). Zero tests passing.</done>
</task>

</tasks>

<verification>
Run `cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/darkMode.test.tsx` — all tests must be RED.
Run `cd /c/Users/gmain/Dev/Notes/client && npx vitest run` — full suite must still pass (other tests unaffected).
</verification>

<success_criteria>
darkMode.test.tsx committed, all 5 tests fail. No regressions in existing test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dark-mode/06-00-SUMMARY.md`
</output>
