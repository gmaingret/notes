---
phase: 06-dark-mode
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - client/src/index.css
  - client/src/components/DocumentView/BulletNode.tsx
  - client/src/components/DocumentView/BulletTree.tsx
  - client/src/components/DocumentView/ContextMenu.tsx
  - client/src/components/DocumentView/DocumentToolbar.tsx
  - client/src/components/DocumentView/DocumentView.tsx
  - client/src/components/DocumentView/FilteredBulletList.tsx
  - client/src/components/DocumentView/FocusToolbar.tsx
  - client/src/components/DocumentView/Lightbox.tsx
  - client/src/components/DocumentView/NoteRow.tsx
  - client/src/components/DocumentView/SearchModal.tsx
  - client/src/components/DocumentView/UndoToast.tsx
  - client/src/components/DocumentView/Breadcrumb.tsx
  - client/src/components/DocumentView/AttachmentRow.tsx
autonomous: true
requirements:
  - DRKM-01
  - DRKM-02

must_haves:
  truths:
    - "All DocumentView components render correctly in both light and dark themes"
    - "No hardcoded hex color values remain in any DocumentView TSX file"
    - "Context menu hover uses CSS :hover rule (no inline onMouseEnter/Leave color assignments)"
    - "Swipe reveal backgrounds reference CSS token vars in the JS expression"
    - "Search highlight in FilteredBulletList uses var(--color-highlight-bg) in the HTML string"
  artifacts:
    - path: "client/src/index.css"
      provides: "All CSS classes for DocumentView component color-bearing elements"
      contains: ".context-menu-item"
    - path: "client/src/components/DocumentView/BulletNode.tsx"
      provides: "Swipe and row bg using token vars"
      contains: "var(--color-swipe-complete)"
    - path: "client/src/components/DocumentView/ContextMenu.tsx"
      provides: "Hover via CSS class, no inline JS color assignment"
      contains: "context-menu-item"
  key_links:
    - from: "client/src/components/DocumentView/FilteredBulletList.tsx"
      to: "var(--color-highlight-bg)"
      via: "dangerouslySetInnerHTML string template"
      pattern: "var\\(--color-highlight-bg\\)"
    - from: "client/src/components/DocumentView/BulletNode.tsx"
      to: "var(--color-swipe-complete)"
      via: "JS ternary expression for swipeBackground"
      pattern: "var\\(--color-swipe"
    - from: "client/src/index.css"
      to: ".context-menu-item:hover"
      via: "CSS :hover rule"
      pattern: "context-menu-item:hover"
---

<objective>
Convert all hardcoded hex colors in DocumentView components to CSS class references using the token system established in Plan 01. Add the corresponding CSS classes to index.css. After this plan, the document viewing area (content, toolbar, context menu, search, breadcrumb, attachments, chips, undo toast) is fully dark-mode-aware.

Purpose: DocumentView is the primary user surface. It must be themed before the checkpoint in Plan 04.

Output: 14 TSX files with zero hardcoded color values; all color classes added to index.css.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dark-mode/06-CONTEXT.md
@.planning/phases/06-dark-mode/06-RESEARCH.md
@client/src/index.css
@client/src/components/DocumentView/BulletNode.tsx
@client/src/components/DocumentView/BulletTree.tsx
@client/src/components/DocumentView/ContextMenu.tsx
@client/src/components/DocumentView/DocumentToolbar.tsx
@client/src/components/DocumentView/DocumentView.tsx
@client/src/components/DocumentView/FilteredBulletList.tsx
@client/src/components/DocumentView/FocusToolbar.tsx
@client/src/components/DocumentView/Lightbox.tsx
@client/src/components/DocumentView/NoteRow.tsx
@client/src/components/DocumentView/SearchModal.tsx
@client/src/components/DocumentView/UndoToast.tsx
@client/src/components/DocumentView/Breadcrumb.tsx
@client/src/components/DocumentView/AttachmentRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert BulletNode, BulletTree, ContextMenu, DocumentToolbar, DocumentView — add CSS classes to index.css</name>
  <files>
    client/src/index.css,
    client/src/components/DocumentView/BulletNode.tsx,
    client/src/components/DocumentView/BulletTree.tsx,
    client/src/components/DocumentView/ContextMenu.tsx,
    client/src/components/DocumentView/DocumentToolbar.tsx,
    client/src/components/DocumentView/DocumentView.tsx
  </files>
  <action>
    For each component, read the file, identify all hardcoded hex/rgba colors in inline `style={}` props, convert to className references, and add the corresponding CSS rules to index.css. Use the RESEARCH.md "Complete Color Audit" section for exact mapping of old values to token vars.

    **BulletNode.tsx changes:**
    - Swipe reveal background: replace the JS ternary that sets a hardcoded color string. New pattern (keep as inline style since it is dynamic):
      ```ts
      background: swipeX > 0 ? 'var(--color-swipe-complete)' : swipeX < 0 ? 'var(--color-swipe-delete)' : 'transparent'
      ```
    - Row content background: `var(--bg, #fff)` → `var(--color-bg-base)` (inline style, keep inline since it is still dynamic if --bg is used elsewhere — but since --bg is never set, just use the token directly)
    - Chevron icon color `#666` → add className `bullet-chevron`; add `.bullet-chevron { color: var(--color-text-muted); }` to index.css
    - Timestamp/dot color `#999` → add className `bullet-timestamp`; add `.bullet-timestamp { color: var(--color-text-faint); font-size: 0.75em; }` to index.css
    - Date label `#d97706` → add className `bullet-date-label`; add `.bullet-date-label { color: var(--color-chip-date-text); }` to index.css
    - Drag overlay indicator `#4A90E2` → `var(--color-accent-blue)` (keep inline if it is a brief inline — or add `.bullet-drag-indicator { color: var(--color-accent-blue); }`)

    **BulletTree.tsx changes:**
    - Placeholder text `#bbb` → add className `bullet-tree-placeholder`; add `.bullet-tree-placeholder { color: var(--color-text-disabled); }` to index.css
    - Drag handle indicator `#4A90E2` → `var(--color-accent-blue)` — use `var()` in the JS expression or a className

    **ContextMenu.tsx changes (CRITICAL — pitfall 3):**
    - Container: `background: 'white'` → `var(--color-bg-raised)`, `border: '1px solid #e0e0e0'` → `var(--color-border-default)`. Keep as inline style for the container box-shadow/position styling, swap only the color values to `var()` references.
    - Remove ALL `onMouseEnter` and `onMouseLeave` handlers that set `e.currentTarget.style.background`. Replace with CSS class.
    - Add className `context-menu-item` to each menu button/div.
    - Add to index.css:
      ```css
      .context-menu-item { padding: 0.5rem 0.75rem; background: transparent; border: none; cursor: pointer; text-align: left; font-size: 0.875rem; color: var(--color-text-secondary); width: 100%; }
      .context-menu-item:hover:not(:disabled) { background: var(--color-bg-surface); }
      .context-menu-item--destructive { color: var(--color-accent-danger); }
      .context-menu-item--disabled { color: var(--color-text-disabled); cursor: default; }
      .context-menu-divider { border-top: 1px solid var(--color-border-default); margin: 0.25rem 0; }
      ```

    **DocumentToolbar.tsx changes:**
    - Bottom border `#f0f0f0` → `var(--color-border-subtle)` (inline style → keep inline with var())
    - Icon/text `#666` → add className `toolbar-btn`; add `.toolbar-btn { color: var(--color-text-muted); }` to index.css
    - Active blue `#4A90E2` → add className `toolbar-btn--active`; add `.toolbar-btn--active { color: var(--color-accent-blue); }` to index.css
    - Inactive `#999` → add className `toolbar-btn--inactive`; add `.toolbar-btn--inactive { color: var(--color-text-faint); }` to index.css
    - Destructive `#e55` → add className `toolbar-btn--destructive`; add `.toolbar-btn--destructive { color: var(--color-accent-danger); }` to index.css

    **DocumentView.tsx changes:**
    - Hamburger button color `#666` → add className `doc-hamburger`; add `.doc-hamburger { color: var(--color-text-muted); }` to index.css
    - Document title `#111` → add className `doc-title`; add `.doc-title { color: var(--color-text-primary); }` to index.css

    Add a `/* === DocumentView component classes === */` comment section in index.css to group these additions cleanly.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    BulletNode, BulletTree, ContextMenu, DocumentToolbar, DocumentView contain zero hardcoded hex color strings.
    ContextMenu has no onMouseEnter/Leave color assignments.
    BulletNode swipe background uses var(--color-swipe-complete/delete).
    Full test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert FilteredBulletList, FocusToolbar, Lightbox, NoteRow, SearchModal, UndoToast, Breadcrumb, AttachmentRow — add CSS classes to index.css</name>
  <files>
    client/src/index.css,
    client/src/components/DocumentView/FilteredBulletList.tsx,
    client/src/components/DocumentView/FocusToolbar.tsx,
    client/src/components/DocumentView/Lightbox.tsx,
    client/src/components/DocumentView/NoteRow.tsx,
    client/src/components/DocumentView/SearchModal.tsx,
    client/src/components/DocumentView/UndoToast.tsx,
    client/src/components/DocumentView/Breadcrumb.tsx,
    client/src/components/DocumentView/AttachmentRow.tsx
  </files>
  <action>
    Read each file, convert hardcoded colors to CSS class references, and add rules to index.css. Use RESEARCH.md "Complete Color Audit" for exact mappings.

    **FilteredBulletList.tsx changes (CRITICAL — pitfall 7 for search highlight):**
    - Loading/empty `#999` → className `filtered-list-empty`; add `.filtered-list-empty { color: var(--color-text-faint); }` to index.css
    - Row border `#f0f0f0` → `var(--color-border-subtle)` inline style
    - Bullet dot `#999` → className `filtered-bullet-dot`; add `.filtered-bullet-dot { color: var(--color-text-faint); }` to index.css
    - Content `#333` → className `filtered-bullet-content`; add `.filtered-bullet-content { color: var(--color-text-secondary); }` to index.css
    - Timestamp `#999` → className `filtered-bullet-ts`; add `.filtered-bullet-ts { color: var(--color-text-faint); }` to index.css
    - Navigate icon `#999` → add className `filtered-nav-icon`; add `.filtered-nav-icon { color: var(--color-text-faint); }` to index.css
    - Search highlight (CRITICAL): Find the `dangerouslySetInnerHTML` string where the highlight is built. Replace the hardcoded `style="background:#fff3cd"` inside the HTML string with `style="background:var(--color-highlight-bg);border-radius:2px"`. The `var()` in an inline style attribute in an HTML string works correctly — browsers resolve custom properties from element styles.

    **FocusToolbar.tsx changes:**
    - Background `#fff` → `var(--color-bg-base)` (inline style)
    - Border `#e5e7eb` → `var(--color-border-default)` (inline style)
    - Button default `#444` → className `focus-toolbar-btn`; add `.focus-toolbar-btn { color: var(--color-text-secondary); background: transparent; border: none; cursor: pointer; padding: 0.4rem 0.6rem; border-radius: 4px; }` to index.css
    - Note active `#4A90E2` → add `.focus-toolbar-btn--note-active { color: var(--color-accent-blue); }` to index.css
    - Bookmark active `#d97706` → add `.focus-toolbar-btn--bookmark-active { color: var(--color-accent-amber); }` to index.css
    - Complete active `#22c55e` → add `.focus-toolbar-btn--complete-active { color: var(--color-accent-green); }` to index.css
    - Delete `#e55` → add `.focus-toolbar-btn--delete { color: var(--color-accent-danger); }` to index.css

    **Lightbox.tsx changes:**
    - Overlay `rgba(0,0,0,0.85)` → KEEP as-is per RESEARCH.md decision: lightbox overlay is intentionally opaque regardless of theme. No change needed.

    **NoteRow.tsx changes:**
    - `#888` → className `note-row-text`; add `.note-row-text { color: var(--color-text-muted); }` to index.css

    **SearchModal.tsx changes:**
    - Overlay `rgba(0,0,0,0.4)` → `var(--color-bg-overlay)` (inline style)
    - Modal bg `#fff` → `var(--color-bg-raised)` (inline style)
    - Border `#e0e0e0` → `var(--color-border-default)` (inline style)
    - Text `#333` → className `search-modal-text`; add `.search-modal-text { color: var(--color-text-secondary); }` to index.css
    - Empty state `#999` → className `search-modal-empty`; add `.search-modal-empty { color: var(--color-text-faint); }` to index.css

    **UndoToast.tsx changes:**
    - Background `#333` → `var(--color-bg-raised)` (inline style). In dark mode this becomes `#1f2937` (from token) — still high contrast with white text. Keep text `#fff` as literal (it works in both themes) or replace with `var(--color-text-primary)`.
    - Border color `#fff` (button border) → keep as-is (white border on dark bg works in both themes, since toast bg is always dark/raised).

    **Breadcrumb.tsx changes:**
    - Separator `#aaa` → className `breadcrumb-separator`; add `.breadcrumb-separator { color: var(--color-text-disabled); }` to index.css
    - Ancestor `#666` → className `breadcrumb-ancestor`; add `.breadcrumb-ancestor { color: var(--color-text-muted); }` to index.css
    - Current `#333` → className `breadcrumb-current`; add `.breadcrumb-current { color: var(--color-text-secondary); }` to index.css
    - Ellipsis `#aaa` → className `breadcrumb-ellipsis`; add `.breadcrumb-ellipsis { color: var(--color-text-disabled); }` to index.css

    **AttachmentRow.tsx changes:**
    - Filename `#666` or similar muted color → className `attachment-filename`; add `.attachment-filename { color: var(--color-text-muted); font-size: 0.85em; }` to index.css
    - Any other hardcoded colors → follow same className + token pattern
    (Read the file to confirm exact values before changing)

    Add a `/* === Additional DocumentView classes === */` comment in index.css to group these additions.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    FilteredBulletList search highlight uses `var(--color-highlight-bg)` in the HTML string.
    FocusToolbar, NoteRow, SearchModal, UndoToast, Breadcrumb, AttachmentRow contain zero hardcoded hex colors.
    Full test suite passes.
  </done>
</task>

</tasks>

<verification>
1. `cd /c/Users/gmain/Dev/Notes/client && npx vitest run` — full suite green.
2. Grep check: `grep -rn "#[0-9a-fA-F]\{3,6\}\|rgba(" client/src/components/DocumentView/ --include="*.tsx" | grep -v "//\|0,0,0,0.85"` — result should be empty or only the Lightbox intentional opacity.
3. ContextMenu has no `onMouseEnter` color assignments remaining.
4. FilteredBulletList HTML string contains `var(--color-highlight-bg)`.
</verification>

<success_criteria>
All 14 DocumentView TSX files contain zero hardcoded hex color values. CSS classes added to index.css. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dark-mode/06-02-SUMMARY.md`
</output>
