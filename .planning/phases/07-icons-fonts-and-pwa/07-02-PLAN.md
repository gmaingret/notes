---
phase: 07-icons-fonts-and-pwa
plan: "02"
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - client/package.json
  - client/package-lock.json
  - client/src/components/Sidebar/Sidebar.tsx
  - client/src/components/Sidebar/DocumentRow.tsx
  - client/src/components/DocumentView/DocumentView.tsx
autonomous: true
requirements:
  - VISL-01

must_haves:
  truths:
    - "lucide-react is installed in client/package.json"
    - "Sidebar overflow button shows MoreHorizontal SVG icon"
    - "Sidebar X close button shows X SVG icon"
    - "Sidebar new doc button shows Plus SVG icon"
    - "Sidebar tabs are icon-only: FileText/Tag/Star with title attributes"
    - "DocumentRow overflow button shows MoreHorizontal SVG icon"
    - "DocumentView hamburger shows Menu SVG icon"
  artifacts:
    - path: "client/src/components/Sidebar/Sidebar.tsx"
      provides: "All Sidebar Unicode icons replaced with Lucide components"
    - path: "client/src/components/Sidebar/DocumentRow.tsx"
      provides: "DocumentRow overflow icon replaced with Lucide MoreHorizontal"
    - path: "client/src/components/DocumentView/DocumentView.tsx"
      provides: "Hamburger &#9776; replaced with Lucide Menu"
  key_links:
    - from: "Sidebar.tsx"
      to: "lucide-react"
      via: "named import: Menu, X, Plus, MoreHorizontal, FileText, Tag, Star"
      pattern: "from 'lucide-react'"
    - from: "DocumentRow.tsx"
      to: "lucide-react"
      via: "named import: MoreHorizontal"
      pattern: "from 'lucide-react'"
    - from: "DocumentView.tsx"
      to: "lucide-react"
      via: "named import: Menu"
      pattern: "from 'lucide-react'"
---

<objective>
Install lucide-react and all fontsource dependencies (installed now so Wave 2 font plan can run in parallel with icon plans). Replace all Unicode icons in Sidebar.tsx, DocumentRow.tsx, and DocumentView.tsx with Lucide React components.

Purpose: Lucide icons in sidebar navigation components — these are the most visible icons in the app.
Output: 3 component files with crisp SVG icons, lucide-react installed.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-icons-fonts-and-pwa/07-CONTEXT.md
@.planning/phases/07-icons-fonts-and-pwa/07-RESEARCH.md
@.planning/phases/07-icons-fonts-and-pwa/07-01-SUMMARY.md

<interfaces>
<!-- Current icon characters to replace -->

Sidebar.tsx current Unicode icons:
- line ~39: '⋯' in overflow button → <MoreHorizontal size={20} strokeWidth={1.5} />
- line ~64: '✕' in X close button → <X size={20} strokeWidth={1.5} />
- line ~80: '+' text in new doc button → <Plus size={20} strokeWidth={1.5} />
- line ~80: '🔖' in bookmark tab → tabs become icon-only (see below)

Sidebar tab transformation (icon-only per locked decision):
BEFORE: {tab === 'docs' ? 'Docs' : tab === 'tags' ? 'Tags' : '🔖'}
AFTER: tabs use icon map with title attribute for accessibility
  docs → <FileText size={20} strokeWidth={1.5} /> title="Documents"
  tags → <Tag size={20} strokeWidth={1.5} /> title="Tags"
  bookmarks → <Star size={20} strokeWidth={1.5} /> title="Bookmarks"

DocumentRow.tsx current Unicode icons:
- line ~118: '⋯' in overflow button → <MoreHorizontal size={20} strokeWidth={1.5} />

DocumentView.tsx current Unicode icons:
- line ~89: '&#9776;' (HTML entity for ☰) in hamburger button → <Menu size={20} strokeWidth={1.5} />
  The hamburger button currently has fontSize: '1.25rem' — remove that style prop when swapping to SVG
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm dependencies</name>
  <files>client/package.json, client/package-lock.json</files>
  <action>
    From the client/ directory, install all Phase 7 npm dependencies in one command:

    ```bash
    cd /c/Users/gmain/Dev/Notes/client && npm install lucide-react @fontsource-variable/inter @fontsource-variable/jetbrains-mono && npm install --save-dev sharp
    ```

    Confirm lucide-react, @fontsource-variable/inter, @fontsource-variable/jetbrains-mono appear in package.json dependencies and sharp in devDependencies.

    Do NOT modify any source files in this task.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && node -e "const p = require('./package.json'); console.log(p.dependencies['lucide-react'], p.dependencies['@fontsource-variable/inter'], p.dependencies['@fontsource-variable/jetbrains-mono'], p.devDependencies['sharp'])"</automated>
  </verify>
  <done>All 4 packages appear in package.json with version strings. No install errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace icons in Sidebar.tsx, DocumentRow.tsx, DocumentView.tsx</name>
  <files>
    client/src/components/Sidebar/Sidebar.tsx,
    client/src/components/Sidebar/DocumentRow.tsx,
    client/src/components/DocumentView/DocumentView.tsx
  </files>
  <behavior>
    - Sidebar.tsx: no '⋯', '✕', '+' text icons, no '🔖' text; instead has lucide-react imports (MoreHorizontal, X, Plus, FileText, Tag, Star) and icon-only tabs with title attributes
    - DocumentRow.tsx: no '⋯'; has MoreHorizontal import from lucide-react
    - DocumentView.tsx: no '&#9776;'; has Menu import from lucide-react
  </behavior>
  <action>
    RULES (apply to all 3 files):
    - Never hardcode a color prop on Lucide icons — icon color inherits from CSS `color` via currentColor
    - Always pass size={20} strokeWidth={1.5} on every icon
    - Do NOT add a wrapping span around icons where none existed before

    **Sidebar.tsx changes:**
    1. Add import at top: `import { MoreHorizontal, X, Plus, FileText, Tag, Star } from 'lucide-react';`
    2. Overflow button: replace '⋯' text content with `<MoreHorizontal size={20} strokeWidth={1.5} />`
    3. X close button: replace '✕' text content with `<X size={20} strokeWidth={1.5} />`
    4. New doc button: replace '+' text content with `<Plus size={20} strokeWidth={1.5} />`
    5. Tab buttons: make icon-only. Replace the tab label expression with:
       ```tsx
       const tabIcon = {
         docs: <FileText size={20} strokeWidth={1.5} />,
         tags: <Tag size={20} strokeWidth={1.5} />,
         bookmarks: <Star size={20} strokeWidth={1.5} />,
       } as const;
       ```
       In the tab button JSX, replace the text label with `{tabIcon[tab]}` and add `title={tab === 'docs' ? 'Documents' : tab === 'tags' ? 'Tags' : 'Bookmarks'}` to the button element.

    **DocumentRow.tsx changes:**
    1. Add import: `import { MoreHorizontal } from 'lucide-react';`
    2. Replace '⋯' text content with `<MoreHorizontal size={20} strokeWidth={1.5} />`

    **DocumentView.tsx changes:**
    1. Add import: `import { Menu } from 'lucide-react';`
    2. Replace `&#9776;` text content with `<Menu size={20} strokeWidth={1.5} />`
    3. Remove `fontSize: '1.25rem'` from the hamburger button inline style (SVG handles its own size)
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1 | grep -E "VISL-01|pass|fail|✓|✗|×"</automated>
  </verify>
  <done>
    All VISL-01 tests for Sidebar.tsx, DocumentRow.tsx, and DocumentView.tsx pass (no Unicode chars found in those files). TypeScript compiles without errors: `cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit 2>&1 | head -20`.
  </done>
</task>

</tasks>

<verification>
Run full iconsAndFonts test suite after both tasks:
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1`

VISL-01 tests for Sidebar.tsx, DocumentRow.tsx, DocumentView.tsx should be GREEN.
Remaining tests (BulletNode/FilteredBulletList/AttachmentRow icons, fonts, PWA) remain RED.
TypeScript compile check: `cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
lucide-react, @fontsource-variable/inter, @fontsource-variable/jetbrains-mono, and sharp installed. Sidebar.tsx, DocumentRow.tsx, DocumentView.tsx have no Unicode/emoji interactive icons — all replaced with Lucide React components at size=20, strokeWidth=1.5. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/07-icons-fonts-and-pwa/07-02-SUMMARY.md`
</output>
