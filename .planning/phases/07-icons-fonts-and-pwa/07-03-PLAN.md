---
phase: 07-icons-fonts-and-pwa
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - client/src/components/DocumentView/BulletNode.tsx
  - client/src/components/DocumentView/FilteredBulletList.tsx
  - client/src/components/DocumentView/AttachmentRow.tsx
autonomous: true
requirements:
  - VISL-01

must_haves:
  truths:
    - "BulletNode collapse chevron is ChevronRight rotating 0→90deg via existing CSS"
    - "BulletNode swipe icons show Check (complete) and Trash2 (delete)"
    - "BulletNode bullet bookmark shows Star icon"
    - "FilteredBulletList bookmark star shows Star filled/outline via CSS class"
    - "FilteredBulletList filtered navigation shows ArrowRight icon"
    - "AttachmentRow non-image/non-PDF attachments show Paperclip icon"
  artifacts:
    - path: "client/src/components/DocumentView/BulletNode.tsx"
      provides: "▶, ✅, 🗑️, 🔖 replaced with Lucide components"
    - path: "client/src/components/DocumentView/FilteredBulletList.tsx"
      provides: "★/☆ bookmark replaced with Star + CSS classes, nav icon replaced with ArrowRight"
    - path: "client/src/components/DocumentView/AttachmentRow.tsx"
      provides: "📎 replaced with Paperclip"
  key_links:
    - from: "BulletNode.tsx"
      to: "lucide-react"
      via: "named import: ChevronRight, Check, Trash2, Star"
      pattern: "from 'lucide-react'"
    - from: "FilteredBulletList.tsx"
      to: "lucide-react"
      via: "named import: Star, ArrowRight"
      pattern: "from 'lucide-react'"
    - from: "AttachmentRow.tsx"
      to: "lucide-react"
      via: "named import: Paperclip"
      pattern: "from 'lucide-react'"
    - from: "index.css"
      to: "Star icon fill state"
      via: ".star-filled and .star-outline CSS classes"
      pattern: "star-filled"
---

<objective>
Replace the remaining Unicode/emoji icons in BulletNode.tsx, FilteredBulletList.tsx, and AttachmentRow.tsx with Lucide React SVG components. Add CSS classes for the Star bookmark fill state.

Purpose: Complete VISL-01 — after this plan, zero Unicode interactive icons remain anywhere in the app.
Output: 3 component files with Lucide icons, index.css with Star fill state classes.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-icons-fonts-and-pwa/07-CONTEXT.md
@.planning/phases/07-icons-fonts-and-pwa/07-RESEARCH.md
@.planning/phases/07-icons-fonts-and-pwa/07-02-SUMMARY.md

<interfaces>
<!-- BulletNode.tsx current Unicode icons (from codebase inspection) -->

Line ~205: swipeIcon variable: `swipeX > 0 ? '✅' : swipeX < 0 ? '🗑️' : null`
Line ~252: `{swipeIcon}` rendered in the swipe backing div
Line ~296: `🔖` rendered as bullet bookmark indicator
Line ~370: `<span style={{ ... }}><span>▶</span></span>` collapse chevron

BEFORE (swipe icon):
```tsx
const swipeIcon = swipeX > 0 ? '✅' : swipeX < 0 ? '🗑️' : null;
// ...rendered as:
{swipeIcon}
```

AFTER (swipe icon):
```tsx
import { ChevronRight, Check, Trash2, Star } from 'lucide-react';
// swipe backing element renders conditionally:
{swipeX > 0 && <Check size={20} strokeWidth={1.5} />}
{swipeX < 0 && <Trash2 size={20} strokeWidth={1.5} />}
```

BEFORE (collapse chevron):
```tsx
<span style={{ display: 'inline-block', transform: bullet.isCollapsed ? 'none' : 'rotate(90deg)', transition: 'transform 0.15s ease' }}>▶</span>
```

AFTER (collapse chevron — keep inline style for the rotation, swap inner content):
```tsx
<span className="bullet-chevron" style={{ display: 'inline-block', transform: bullet.isCollapsed ? 'none' : 'rotate(90deg)', transition: 'transform 0.15s ease' }}>
  <ChevronRight size={16} strokeWidth={1.5} />
</span>
```
Note: ChevronRight uses size={16} (not 20) — it sits inside a bullet row alongside text, 20px would be too large relative to the bullet dot.

BEFORE (bookmark in BulletNode):
```tsx
🔖
```
AFTER:
```tsx
<Star size={16} strokeWidth={1.5} className="star-filled" />
```
The bullet bookmark is always in filled state (only shown when bookmarked). Use size={16} to match the chevron scale in bullet rows.

<!-- FilteredBulletList.tsx current Unicode icons -->

Line ~98-100: bookmark toggle button renders '★' when bookmarked, '☆' when not
Line (nav icon): filtered navigation icon (arrow) — check actual icon in file

BEFORE (bookmark):
```tsx
{row.isBookmarked ? '★' : '☆'}
```
AFTER:
```tsx
<Star
  size={16}
  strokeWidth={1.5}
  className={row.isBookmarked ? 'star-filled' : 'star-outline'}
/>
```

BEFORE (nav icon — check actual character in file):
Whatever character is currently used for the filtered navigation arrow → <ArrowRight size={16} strokeWidth={1.5} />

<!-- AttachmentRow.tsx current Unicode icons -->

Line ~136: `<span style={{ fontSize: '1.1em' }}>📎</span>` — generic file icon
AFTER: `<Paperclip size={20} strokeWidth={1.5} />`
Remove the wrapping span (it only existed to size the emoji). Add Paperclip import.

<!-- CSS classes needed in index.css for Star fill state -->

Add to index.css (near .chip or .bookmark-star section):
```css
.star-filled { fill: var(--color-accent-amber); color: var(--color-accent-amber); }
.star-outline { fill: none; color: var(--color-text-muted); }
```
These classes control whether the Star icon renders filled (bookmarked) or outlined (not bookmarked).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace BulletNode icons and add Star CSS classes</name>
  <files>
    client/src/components/DocumentView/BulletNode.tsx,
    client/src/index.css
  </files>
  <behavior>
    - BulletNode.tsx: no '▶', '✅', '🗑️', '🔖' characters; has ChevronRight/Check/Trash2/Star imports from lucide-react
    - index.css: has .star-filled and .star-outline rules with CSS token colors
  </behavior>
  <action>
    **BulletNode.tsx:**
    1. Add import: `import { ChevronRight, Check, Trash2, Star } from 'lucide-react';`
    2. Remove the `swipeIcon` variable entirely.
    3. In the swipe backing div, replace `{swipeIcon}` with:
       ```tsx
       {swipeX > 0 && <Check size={20} strokeWidth={1.5} />}
       {swipeX < 0 && <Trash2 size={20} strokeWidth={1.5} />}
       ```
    4. Replace the collapse chevron `▶` span content with `<ChevronRight size={16} strokeWidth={1.5} />`. Keep the existing inline style for the rotation/transition on the outer span — do NOT change the rotation logic.
    5. Replace `🔖` bookmark character with `<Star size={16} strokeWidth={1.5} className="star-filled" />`.

    **index.css:**
    Add after the `.chip` rules section (around line 141):
    ```css
    .star-filled { fill: var(--color-accent-amber); color: var(--color-accent-amber); }
    .star-outline { fill: none; color: var(--color-text-muted); }
    ```

    CRITICAL: Never add a `color` prop directly to Lucide icons — always use className or inherit from parent CSS.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1 | grep -E "BulletNode|VISL-01|pass|fail|✓|✗|×"</automated>
  </verify>
  <done>BulletNode.tsx contains no '▶', '✅', '🗑️', '🔖'. index.css has .star-filled and .star-outline rules. TypeScript compiles: `npx tsc --noEmit 2>&1 | head -20`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace FilteredBulletList and AttachmentRow icons</name>
  <files>
    client/src/components/DocumentView/FilteredBulletList.tsx,
    client/src/components/DocumentView/AttachmentRow.tsx
  </files>
  <behavior>
    - FilteredBulletList.tsx: no '★', '☆' characters; Star import from lucide-react; bookmark button uses className="star-filled" or "star-outline"
    - AttachmentRow.tsx: no '📎' character; Paperclip import from lucide-react
  </behavior>
  <action>
    **FilteredBulletList.tsx:**
    1. Add import: `import { Star, ArrowRight } from 'lucide-react';`
    2. Replace `{row.isBookmarked ? '★' : '☆'}` with:
       ```tsx
       <Star
         size={16}
         strokeWidth={1.5}
         className={row.isBookmarked ? 'star-filled' : 'star-outline'}
       />
       ```
    3. Find the filtered navigation icon (whatever character currently marks "navigate to filtered view") and replace with `<ArrowRight size={16} strokeWidth={1.5} />`. Read the file first to identify the exact character.

    **AttachmentRow.tsx:**
    1. Add import: `import { Paperclip } from 'lucide-react';`
    2. Find the generic file attachment row (the non-image, non-PDF branch that renders `<span style={{ fontSize: '1.1em' }}>📎</span>`).
    3. Replace that span entirely with `<Paperclip size={20} strokeWidth={1.5} />`.

    After both edits, run the full iconsAndFonts test suite to confirm all VISL-01 tests now pass.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1</automated>
  </verify>
  <done>
    All VISL-01 tests pass (all 6 component files clear of Unicode interactive icons). Remaining failing tests are VISL-02, VISL-03, PWA-01, PWA-02, PWA-03 (fonts and PWA not yet implemented). TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
After both tasks, all VISL-01 tests in iconsAndFonts.test.ts should be GREEN:
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1`

Zero Unicode interactive icons remain in any component file. TypeScript compiles without errors:
`cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
BulletNode.tsx, FilteredBulletList.tsx, AttachmentRow.tsx contain no Unicode/emoji interactive icons. All replaced with Lucide React components at appropriate sizes (16px for in-line bullet icons, 20px for standalone action icons). Star fill state controlled via .star-filled/.star-outline CSS classes using design token colors.
</success_criteria>

<output>
After completion, create `.planning/phases/07-icons-fonts-and-pwa/07-03-SUMMARY.md`
</output>
