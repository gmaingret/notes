---
phase: 07-icons-fonts-and-pwa
plan: "04"
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - client/src/main.tsx
  - client/src/index.css
autonomous: true
requirements:
  - VISL-02
  - VISL-03

must_haves:
  truths:
    - "UI text renders in Inter variable font loaded from the app server (no Google Fonts)"
    - "Inline code, tag chips, search input, and comment text use JetBrains Mono variable font"
  artifacts:
    - path: "client/src/main.tsx"
      provides: "fontsource imports for Inter Variable and JetBrains Mono Variable at top of file"
      contains: "@fontsource-variable/inter"
    - path: "client/src/index.css"
      provides: "body uses 'Inter Variable' sans-serif; .chip, .note-row-text, search input, inline code use 'JetBrains Mono Variable'"
      contains: "'Inter Variable', sans-serif"
  key_links:
    - from: "client/src/main.tsx"
      to: "@fontsource-variable/inter"
      via: "import at top of file before './index.css'"
      pattern: "@fontsource-variable/inter"
    - from: "client/src/index.css"
      to: "'Inter Variable'"
      via: "body font-family replacement"
      pattern: "Inter Variable"
    - from: "client/src/index.css"
      to: "'JetBrains Mono Variable'"
      via: ".chip rule + mono CSS class"
      pattern: "JetBrains Mono Variable"
---

<objective>
Wire the self-hosted variable fonts into the app. Add fontsource imports to main.tsx, replace the system font stack in index.css body rule with Inter Variable, and add JetBrains Mono Variable to chip/code/search/comment elements.

Purpose: Satisfy VISL-02 and VISL-03 — Inter as the UI font and JetBrains Mono for code/chips, both loaded from the app server with no Google Fonts requests.
Output: main.tsx with fontsource imports, index.css with updated font rules.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-icons-fonts-and-pwa/07-CONTEXT.md
@.planning/phases/07-icons-fonts-and-pwa/07-RESEARCH.md
@.planning/phases/07-icons-fonts-and-pwa/07-02-SUMMARY.md

<interfaces>
<!-- From client/src/index.css -->

Current body rule (line ~155):
```css
body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; }
```
Replace the font-family value with: `'Inter Variable', sans-serif`
Keep `margin: 0` unchanged.

Current .chip rule (line ~141):
```css
.chip { display: inline-block; border-radius: 3px; padding: 0 4px; font-size: 0.85em; cursor: pointer; font-weight: 500; }
```
Add `font-family: 'JetBrains Mono Variable', monospace;` to this rule.

<!-- JetBrains Mono scope from CONTEXT.md locked decisions -->
Apply 'JetBrains Mono Variable', monospace to:
1. .chip (chips already have this class — adding font-family here covers all tag/mention/date chips)
2. Inline code spans — these are rendered inside bullet content; check BulletContent.tsx for the CSS class used for inline code spans
3. Search input — SearchModal.tsx uses an inline-styled <input>; add a CSS class 'search-modal-input' and add JetBrains Mono to it
4. Comment (note) body text — NoteRow.tsx renders a div with className="note-row-text"; add JetBrains Mono to that class

<!-- From client/src/main.tsx (imports order) -->
The fontsource imports MUST appear before the './index.css' import.
Confirmed pattern from @fontsource docs:
```tsx
import '@fontsource-variable/inter';
import '@fontsource-variable/jetbrains-mono';
import './index.css';  // this line must come AFTER fontsource imports
```

<!-- Font name pitfall (from RESEARCH.md) -->
Use EXACTLY these names in CSS:
- 'Inter Variable' (NOT 'Inter', NOT 'InterVariable')
- 'JetBrains Mono Variable' (NOT 'JetBrains Mono', NOT 'JetBrainsMono Variable')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fontsource imports to main.tsx</name>
  <files>client/src/main.tsx</files>
  <behavior>
    - main.tsx contains `import '@fontsource-variable/inter';` before the './index.css' import
    - main.tsx contains `import '@fontsource-variable/jetbrains-mono';` before the './index.css' import
  </behavior>
  <action>
    Read client/src/main.tsx to find the current import order.
    Add these two imports at the very top of the file, before all other imports including React:
    ```tsx
    import '@fontsource-variable/inter';
    import '@fontsource-variable/jetbrains-mono';
    ```

    The RESEARCH.md recommends placing them before './index.css'. Place them as the first two lines of the file.

    Do NOT add any other changes to main.tsx.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1 | grep -E "VISL-02|VISL-03|inter|jetbrains|pass|fail|✓|✗|×"</automated>
  </verify>
  <done>VISL-02 and VISL-03 tests for main.tsx imports pass (both imports found in file). TypeScript compiles without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update index.css font rules for Inter and JetBrains Mono</name>
  <files>client/src/index.css</files>
  <behavior>
    - body font-family contains 'Inter Variable', sans-serif
    - .chip rule contains 'JetBrains Mono Variable', monospace
    - .note-row-text rule contains 'JetBrains Mono Variable', monospace
    - A .search-modal-input class exists with 'JetBrains Mono Variable', monospace
    - Inline code elements have a CSS class with 'JetBrains Mono Variable', monospace
  </behavior>
  <action>
    **1. Update body font-family (line ~155):**
    Replace:
    ```css
    body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; }
    ```
    With:
    ```css
    body { margin: 0; font-family: 'Inter Variable', sans-serif; }
    ```

    **2. Add JetBrains Mono to .chip rule (line ~141):**
    Add `font-family: 'JetBrains Mono Variable', monospace;` to the existing .chip rule.
    Result:
    ```css
    .chip { display: inline-block; border-radius: 3px; padding: 0 4px; font-size: 0.85em; cursor: pointer; font-weight: 500; font-family: 'JetBrains Mono Variable', monospace; }
    ```

    **3. Add JetBrains Mono to .note-row-text (comment body text):**
    Find or add the .note-row-text rule and add `font-family: 'JetBrains Mono Variable', monospace;`. NoteRow.tsx uses className="note-row-text" — this class likely already exists in index.css or add it if not.

    **4. Add .search-modal-input class for the search input:**
    Add a new rule:
    ```css
    .search-modal-input { font-family: 'JetBrains Mono Variable', monospace; }
    ```
    Then update SearchModal.tsx: add `className="search-modal-input"` to the `<input>` element alongside the existing inline styles (inline styles win over class for other properties, so this is safe).

    **5. Check BulletContent.tsx for inline code class:**
    Read BulletContent.tsx to identify the CSS class applied to inline code spans (backtick markdown). Add `font-family: 'JetBrains Mono Variable', monospace;` to that class in index.css. If inline code uses an inline `style` prop, add a CSS class instead and apply font-family there.

    If files need to be updated beyond index.css (SearchModal.tsx, BulletContent.tsx), update them in this task — they are small changes (adding a className).
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1</automated>
  </verify>
  <done>
    All VISL-02 and VISL-03 tests pass. Body uses 'Inter Variable'. .chip, .note-row-text, .search-modal-input, and inline code class all use 'JetBrains Mono Variable'. TypeScript compiles without errors: `cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit 2>&1 | head -20`
  </done>
</task>

</tasks>

<verification>
After both tasks, VISL-02 and VISL-03 tests are GREEN:
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run --reporter=verbose src/test/iconsAndFonts.test.ts 2>&1`

Check no Google Fonts requests: `grep -r "fonts.google\|googleapis.com/css" client/src/ client/index.html` — should return empty.
TypeScript compiles: `cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
Inter Variable is the UI font for the app body, loaded via @fontsource-variable/inter (no CDN). JetBrains Mono Variable is applied to all chip/code/search/comment elements via CSS classes. No Google Fonts or external font CDN requests exist anywhere in the codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/07-icons-fonts-and-pwa/07-04-SUMMARY.md`
</output>
