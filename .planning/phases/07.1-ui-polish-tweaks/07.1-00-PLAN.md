---
phase: 07.1-ui-polish-tweaks
plan: "00"
type: tdd
wave: 1
depends_on: []
files_modified:
  - client/src/test/uiPolish.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "uiPolish.test.ts exists and all assertions FAIL (RED) before implementation"
    - "Each of the four polish behaviors has at least one automated static-scan assertion"
  artifacts:
    - path: "client/src/test/uiPolish.test.ts"
      provides: "Static file-scan tests for all Phase 7.1 behaviors"
      exports: []
  key_links:
    - from: "client/src/test/uiPolish.test.ts"
      to: "client/src/components/DocumentView/FocusToolbar.tsx"
      via: "readFileSync + string assertions"
      pattern: "readFileSync.*FocusToolbar"
    - from: "client/src/test/uiPolish.test.ts"
      to: "client/src/components/Sidebar/Sidebar.tsx"
      via: "readFileSync + string assertions"
      pattern: "readFileSync.*Sidebar"
---

<objective>
Create the Wave 0 test scaffold for Phase 7.1 — static file-scan tests covering all four polish behaviors, written in RED state before any implementation.

Purpose: Establish a verification contract that turns GREEN exactly when each implementation task completes. Follows the iconsAndFonts.test.ts pattern already established in the project.
Output: client/src/test/uiPolish.test.ts — RED on creation, GREEN after all implementation plans complete.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-ui-polish-tweaks/07.1-CONTEXT.md
@.planning/phases/07.1-ui-polish-tweaks/07.1-RESEARCH.md

The existing iconsAndFonts.test.ts is the template to follow exactly:
- Uses `process.cwd()` for path resolution (Vitest runs from client/ dir)
- Uses `readFileSync` to load source files at top-level
- Assertions use `.toContain()` / `.not.toContain()` on file content strings
- No jsdom, no React rendering — pure static analysis
</context>

<interfaces>
<!-- Template pattern from client/src/test/iconsAndFonts.test.ts -->
```typescript
import { readFileSync } from 'fs';
import { resolve } from 'path';
import { describe, it, expect } from 'vitest';

const root = process.cwd(); // = client/ when run via npx vitest
const somePath = resolve(root, 'src/components/...');
const someFile = readFileSync(somePath, 'utf-8');

describe('Behavior description', () => {
  it('specific assertion', () => {
    expect(someFile).not.toContain('bad string');
    expect(someFile).toContain('good string');
  });
});
```

<!-- Files the tests will scan: -->
- FocusToolbar.tsx currently has: &#8677; &#8676; &#8593; &#8595; &#8617; &#8618; &#128206; &#128172; &#128278; &#10003; &#128465;
- BulletNode.tsx outer div currently has: paddingLeft: depth * 24 (NO paddingTop/paddingBottom yet)
- Sidebar.tsx currently has: showSidebarMenu state, MoreHorizontal button, dropdownStyle const
- DocumentList.tsx Props type currently is: { activeDocId: string | null } (no pendingRenameId)
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test scaffold — uiPolish.test.ts</name>
  <files>client/src/test/uiPolish.test.ts</files>
  <action>
Create client/src/test/uiPolish.test.ts following the exact pattern of iconsAndFonts.test.ts.

File paths to resolve (all relative to process.cwd() = client/):
- focusToolbarPath: src/components/DocumentView/FocusToolbar.tsx
- bulletNodePath: src/components/DocumentView/BulletNode.tsx
- sidebarPath: src/components/Sidebar/Sidebar.tsx
- documentListPath: src/components/Sidebar/DocumentList.tsx

Load all four files with readFileSync at module top level (not inside describe blocks).

Write the following describe blocks and assertions:

**describe('Phase 7.1 — FocusToolbar: no HTML entity characters')**
- 'FocusToolbar.tsx does NOT contain &#8677; (indent entity)' → expect(focusToolbar).not.toContain('&#8677;')
- 'FocusToolbar.tsx does NOT contain &#8676; (outdent entity)' → expect(focusToolbar).not.toContain('&#8676;')
- 'FocusToolbar.tsx does NOT contain &#8593; (arrow up entity)' → expect(focusToolbar).not.toContain('&#8593;')
- 'FocusToolbar.tsx does NOT contain &#8595; (arrow down entity)' → expect(focusToolbar).not.toContain('&#8595;')
- 'FocusToolbar.tsx does NOT contain &#8617; (undo entity)' → expect(focusToolbar).not.toContain('&#8617;')
- 'FocusToolbar.tsx does NOT contain &#8618; (redo entity)' → expect(focusToolbar).not.toContain('&#8618;')
- 'FocusToolbar.tsx does NOT contain &#128206; (attach entity)' → expect(focusToolbar).not.toContain('&#128206;')
- 'FocusToolbar.tsx does NOT contain &#128172; (note entity)' → expect(focusToolbar).not.toContain('&#128172;')
- 'FocusToolbar.tsx does NOT contain &#128278; (bookmark entity)' → expect(focusToolbar).not.toContain('&#128278;')
- 'FocusToolbar.tsx does NOT contain &#10003; (check entity)' → expect(focusToolbar).not.toContain('&#10003;')
- 'FocusToolbar.tsx does NOT contain &#128465; (delete entity)' → expect(focusToolbar).not.toContain('&#128465;')
- 'FocusToolbar.tsx imports lucide-react (IndentIncrease and Undo2)' → expect(focusToolbar).toContain('IndentIncrease') and expect(focusToolbar).toContain('Undo2')

**describe('Phase 7.1 — BulletNode: outer row has 2px vertical padding')**
- 'BulletNode.tsx outer row div has paddingTop: 2' → expect(bulletNode).toContain('paddingTop: 2')
- 'BulletNode.tsx outer row div has paddingBottom: 2' → expect(bulletNode).toContain('paddingBottom: 2')

**describe('Phase 7.1 — Sidebar: footer replaces ... dropdown')**
- 'Sidebar.tsx does NOT contain showSidebarMenu state' → expect(sidebar).not.toContain('showSidebarMenu')
- 'Sidebar.tsx does NOT contain dropdownStyle const' → expect(sidebar).not.toContain('dropdownStyle')
- 'Sidebar.tsx imports Upload (footer export button)' → expect(sidebar).toContain('Upload')
- 'Sidebar.tsx imports LogOut (footer logout button)' → expect(sidebar).toContain('LogOut')
- 'Sidebar.tsx contains sidebar-footer-btn CSS class' → expect(sidebar).toContain('sidebar-footer-btn')

**describe('Phase 7.1 — DocumentList: accepts pendingRenameId prop')**
- 'DocumentList.tsx Props type contains pendingRenameId' → expect(documentList).toContain('pendingRenameId')
- 'DocumentList.tsx passes initiallyRenaming to DocumentRow' → expect(documentList).toContain('initiallyRenaming')

After writing the file, run the test suite to confirm all tests FAIL (RED state is required):
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/uiPolish.test.ts`

All tests should fail because the source files have not been changed yet. If any test passes already, the assertion is too weak — tighten it.

Commit with: `test(07.1-00): add failing test scaffold — uiPolish.test.ts RED`
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/uiPolish.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>uiPolish.test.ts exists, all tests FAIL (RED) because source files not yet modified. Test count matches the assertion list above. Full suite still passes (other tests unaffected): `cd /c/Users/gmain/Dev/Notes/client && npx vitest run`</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no existing tests broken by the new file:
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run`

Confirm uiPolish.test.ts fails with meaningful failure messages (not import errors or file-not-found):
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/uiPolish.test.ts 2>&1 | grep -E "FAIL|PASS|Error"`
</verification>

<success_criteria>
- uiPolish.test.ts exists at client/src/test/uiPolish.test.ts
- All tests in uiPolish.test.ts FAIL (the source files still have HTML entities, no paddingTop/paddingBottom, showSidebarMenu present, no pendingRenameId)
- No existing tests broken (full suite green except uiPolish.test.ts)
- Committed on phase branch
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ui-polish-tweaks/07.1-00-SUMMARY.md`
</output>
