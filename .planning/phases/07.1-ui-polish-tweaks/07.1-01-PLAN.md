---
phase: 07.1-ui-polish-tweaks
plan: "01"
type: execute
wave: 2
depends_on:
  - "07.1-00"
files_modified:
  - client/src/components/DocumentView/FocusToolbar.tsx
  - client/src/components/DocumentView/BulletNode.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "FocusToolbar buttons show crisp SVG icons — no browser-rendered HTML entity glyphs"
    - "Bullet rows have 2px vertical breathing room above and below each row"
  artifacts:
    - path: "client/src/components/DocumentView/FocusToolbar.tsx"
      provides: "All 11 toolbar buttons using Lucide icons"
      contains: "IndentIncrease"
    - path: "client/src/components/DocumentView/BulletNode.tsx"
      provides: "Outer row div with paddingTop/paddingBottom: 2"
      contains: "paddingTop: 2"
  key_links:
    - from: "FocusToolbar.tsx"
      to: "lucide-react"
      via: "import { IndentIncrease, IndentDecrease, ArrowUp, ArrowDown, Undo2, Redo2, Paperclip, StickyNote, Bookmark, Check, Trash2 } from 'lucide-react'"
      pattern: "from 'lucide-react'"
    - from: "BulletNode.tsx outer div"
      to: "row spacing"
      via: "paddingTop: 2, paddingBottom: 2 on the dnd-kit ref div"
      pattern: "paddingTop: 2"
---

<objective>
Replace all 11 HTML entity characters in FocusToolbar with Lucide SVG icons, and add 2px vertical padding to the BulletNode outer row div.

Purpose: Complete VISL-01 compliance for FocusToolbar (last component with HTML entities) and improve bullet readability with Notion/Logseq-style row density.
Output: FocusToolbar.tsx with Lucide imports, BulletNode.tsx with 2px row padding.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-ui-polish-tweaks/07.1-CONTEXT.md
@.planning/phases/07.1-ui-polish-tweaks/07.1-RESEARCH.md
</context>

<interfaces>
<!-- Current FocusToolbar.tsx — all 11 HTML entities to replace -->
<!-- Line 176: &#8677; → <IndentIncrease size={20} strokeWidth={1.5} /> -->
<!-- Line 184: &#8676; → <IndentDecrease size={20} strokeWidth={1.5} /> -->
<!-- Line 192: &#8593; → <ArrowUp size={20} strokeWidth={1.5} /> -->
<!-- Line 200: &#8595; → <ArrowDown size={20} strokeWidth={1.5} /> -->
<!-- Line 203: &#8617; → <Undo2 size={20} strokeWidth={1.5} /> -->
<!-- Line 206: &#8618; → <Redo2 size={20} strokeWidth={1.5} /> -->
<!-- Line 209: &#128206; → <Paperclip size={20} strokeWidth={1.5} /> -->
<!-- Line 218: &#128172; → <StickyNote size={20} strokeWidth={1.5} /> -->
<!-- Line 226: &#128278; → <Bookmark size={20} strokeWidth={1.5} /> -->
<!-- Line 233: &#10003; → <Check size={20} strokeWidth={1.5} /> -->
<!-- Line 236: &#128465; → <Trash2 size={20} strokeWidth={1.5} /> -->

<!-- FocusToolbar.tsx has NO lucide-react import yet — it imports from react and tanstack -->
<!-- BulletNode.tsx outer div (line ~207-223): -->
```
style={{
  position: 'relative',
  display: 'flex',
  alignItems: 'flex-start',
  paddingLeft: depth * 24,
  textDecoration: 'none',
  touchAction: 'pan-y',
  overflow: 'hidden',
  ...style,
  opacity: bullet.isComplete ? 0.5 : style.opacity,
}}
```
<!-- Add paddingTop: 2, paddingBottom: 2 to this style object (before the ...style spread) -->
<!-- IMPORTANT: This is the OUTER div (dnd-kit ref div), NOT the inner translated content div -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace all 11 HTML entities in FocusToolbar.tsx with Lucide icons</name>
  <files>client/src/components/DocumentView/FocusToolbar.tsx</files>
  <action>
Add a lucide-react import at the top of FocusToolbar.tsx (after the existing react import, before the other imports):

```typescript
import {
  IndentIncrease, IndentDecrease,
  ArrowUp, ArrowDown,
  Undo2, Redo2,
  Paperclip, StickyNote,
  Bookmark, Check, Trash2,
} from 'lucide-react';
```

Replace all 11 HTML entity literals in the button JSX:
- &#8677; → `<IndentIncrease size={20} strokeWidth={1.5} />`
- &#8676; → `<IndentDecrease size={20} strokeWidth={1.5} />`
- &#8593; → `<ArrowUp size={20} strokeWidth={1.5} />`
- &#8595; → `<ArrowDown size={20} strokeWidth={1.5} />`
- &#8617; → `<Undo2 size={20} strokeWidth={1.5} />`
- &#8618; → `<Redo2 size={20} strokeWidth={1.5} />`
- &#128206; → `<Paperclip size={20} strokeWidth={1.5} />`
- &#128172; → `<StickyNote size={20} strokeWidth={1.5} />`
- &#128278; → `<Bookmark size={20} strokeWidth={1.5} />`
- &#10003; → `<Check size={20} strokeWidth={1.5} />`
- &#128465; → `<Trash2 size={20} strokeWidth={1.5} />`

No other changes to FocusToolbar.tsx. The className, style, onClick, title, and conditional opacity patterns remain unchanged. The file already imports `useState` and `useEffect` from react — Lucide import goes between that and the `useQueryClient` import or after it, either position is fine.

Note: BulletNode.tsx already imports Check and Trash2 — that is a different file. FocusToolbar.tsx has zero lucide-react usage currently.

After editing, run the test to verify this task's assertions pass:
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/uiPolish.test.ts --reporter=verbose 2>&1 | grep -E "FocusToolbar|✓|✗|FAIL|PASS"`

Commit: `feat(07.1-01): replace FocusToolbar HTML entities with Lucide icons`
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/uiPolish.test.ts 2>&1 | grep -E "FocusToolbar"</automated>
  </verify>
  <done>All 12 FocusToolbar assertions in uiPolish.test.ts pass. No HTML entity strings remain in FocusToolbar.tsx. TypeScript compiles without errors (`npx tsc --noEmit` in client/).</done>
</task>

<task type="auto">
  <name>Task 2: Add 2px vertical padding to BulletNode outer row div</name>
  <files>client/src/components/DocumentView/BulletNode.tsx</files>
  <action>
Find the outer row div in BulletNode.tsx — it is the div with `ref` attached to both the dnd-kit `setNodeRef` and the `rowRef`, identified by its style containing `paddingLeft: depth * 24`. This is the dnd-kit sortable container div, NOT the inner translated content div.

Add `paddingTop: 2` and `paddingBottom: 2` to that div's inline style object. Place them after `paddingLeft: depth * 24` and before `textDecoration: 'none'`:

```typescript
style={{
  position: 'relative',
  display: 'flex',
  alignItems: 'flex-start',
  paddingLeft: depth * 24,
  paddingTop: 2,
  paddingBottom: 2,
  textDecoration: 'none',
  touchAction: 'pan-y',
  overflow: 'hidden',
  ...style,
  opacity: bullet.isComplete ? 0.5 : style.opacity,
}}
```

Do NOT add padding to the inner translated div (the one with `transform: translateX(swipeX)` or similar). Bullet spacing belongs only on the outer dnd-kit ref div.

No other changes to BulletNode.tsx.

Commit: `feat(07.1-01): add 2px vertical padding to BulletNode rows`
  </action>
  <verify>
    <automated>cd /c/Users/gmain/Dev/Notes/client && npx vitest run src/test/uiPolish.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>BulletNode assertions in uiPolish.test.ts pass (paddingTop: 2 and paddingBottom: 2 present). All other uiPolish tests still fail (Plan 02 not yet implemented). Full suite: `npx vitest run` shows only uiPolish.test.ts failures remaining from Plan 02 scope.</done>
</task>

</tasks>

<verification>
Run full vitest suite to confirm no regressions:
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run`

Run TypeScript check:
`cd /c/Users/gmain/Dev/Notes/client && npx tsc --noEmit`

Expected: FocusToolbar + BulletNode describe blocks in uiPolish.test.ts are GREEN. Sidebar + DocumentList blocks still RED (Plan 02 not run yet). All other test files GREEN.
</verification>

<success_criteria>
- FocusToolbar.tsx has zero HTML entity strings (all 11 replaced with Lucide components)
- BulletNode.tsx outer row div has paddingTop: 2 and paddingBottom: 2
- TypeScript compiles without errors
- uiPolish.test.ts FocusToolbar and BulletNode describe blocks: GREEN
- No existing tests broken
- Both changes committed on phase branch
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ui-polish-tweaks/07.1-01-SUMMARY.md`
</output>
