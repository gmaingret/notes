---
phase: 07.1-ui-polish-tweaks
plan: "02"
subsystem: ui
tags: [react, lucide-react, sidebar, css-tokens, tanstack-query]

# Dependency graph
requires:
  - phase: 07.1-ui-polish-tweaks
    provides: "TDD wave 0 test scaffold for all Phase 7.1 behaviors (uiPolish.test.ts)"
provides:
  - "Sidebar persistent footer with Export all and Logout always-visible below scrollable list"
  - "... dropdown removed from sidebar header"
  - "+ button creates document, switches to Docs tab, triggers inline rename on new row"
  - "pendingRenameId state prop threading from Sidebar -> DocumentList -> DocumentRow"
  - "initiallyRenaming prop seeds DocumentRow useState for auto-focus rename input"
affects: [sidebar, document-creation, mobile-layout]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Footer buttons use CSS class hover rule with design tokens (not inline onMouseEnter/Leave) — consistent with Phase 6 dark mode pattern"
    - "TanStack Query v5 mutate onSuccess callback sets local state for coordinated UI transitions"
    - "initiallyRenaming prop seeds useState on mount — no useEffect needed, React Query invalidation re-render timing handles it"

key-files:
  created: []
  modified:
    - client/src/components/Sidebar/Sidebar.tsx
    - client/src/components/Sidebar/DocumentList.tsx
    - client/src/components/Sidebar/DocumentRow.tsx
    - client/src/index.css

key-decisions:
  - "Footer buttons use sidebar-footer-btn CSS class with .sidebar-footer-btn:hover rule using var(--color-bg-surface) — CSS token hover for dark mode correctness (established Phase 6 pattern)"
  - "pendingRenameId state in Sidebar threaded as prop to DocumentList and DocumentRow — no global store needed, local React state sufficient for single-use rename trigger"

patterns-established:
  - "CSS token hover pattern: always use CSS class + :hover rule, never inline onMouseEnter/Leave"
  - "Mutate onSuccess for coordinated post-creation UI: set pendingRenameId in onSuccess callback after React Query cache invalidated"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-03-10
---

# Phase 07.1 Plan 02: Sidebar Footer and Inline Rename on Creation Summary

**Sidebar ... dropdown replaced with persistent Export all/Logout footer; + button now auto-opens inline rename on the newly created document row**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-10T20:10:00Z
- **Completed:** 2026-03-10T20:22:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Removed `showSidebarMenu` state and `MoreHorizontal` dropdown from Sidebar header
- Added always-visible footer with Upload (Export all) and LogOut icons using `footerBtnBase` inline style + `.sidebar-footer-btn:hover` CSS token rule
- Wired `+` button to switch to Docs tab, then call `createDocument` with `onSuccess` callback setting `pendingRenameId`
- Threaded `pendingRenameId` and `onRenameComplete` props through `DocumentList` to `DocumentRow`
- `DocumentRow` now seeds `isRenaming` useState from `initiallyRenaming` prop, auto-focusing rename input on new document creation

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove ... dropdown, add sidebar footer with Export/Logout** - `70bc943` (feat)
2. **Task 2: Thread pendingRenameId through DocumentList and DocumentRow** - `35e276f` (feat)

## Files Created/Modified
- `client/src/components/Sidebar/Sidebar.tsx` - Removed dropdown, added pendingRenameId state, footer with Upload/LogOut, updated DocumentList props
- `client/src/components/Sidebar/DocumentList.tsx` - Extended Props type with pendingRenameId/onRenameComplete, passes initiallyRenaming to DocumentRow
- `client/src/components/Sidebar/DocumentRow.tsx` - Extended Props type with initiallyRenaming/onRenameComplete, seeds isRenaming useState, calls onRenameComplete in handleRenameSubmit
- `client/src/index.css` - Added `.sidebar-footer-btn:hover { background: var(--color-bg-surface); }` rule

## Decisions Made
- CSS class hover rule used for footer buttons (not inline JS) — aligns with Phase 6 dark mode pattern where CSS tokens must resolve correctly in both themes
- `pendingRenameId` held in Sidebar local state via `useState<string | null>(null)` — cleared by `onRenameComplete` callback, no global store needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing 5 failures in `mobileLayout.test.tsx` (MOBL-01, MOBL-02, MOBL-03 tests) confirmed to exist before any changes in this plan — out of scope, logged to deferred items.

## Next Phase Readiness
- Both Phase 7.1 plans complete: all 21 uiPolish.test.ts assertions GREEN, TypeScript clean
- Ready for phase PR review and merge

---
*Phase: 07.1-ui-polish-tweaks*
*Completed: 2026-03-10*
