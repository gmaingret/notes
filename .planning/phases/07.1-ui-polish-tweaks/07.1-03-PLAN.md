---
phase: 07.1-ui-polish-tweaks
plan: "03"
type: execute
wave: 3
depends_on:
  - "07.1-01"
  - "07.1-02"
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "FocusToolbar shows crisp SVG icons on a real mobile viewport"
    - "Bullet rows have visible spacing — feels like Notion density"
    - "Sidebar footer shows Export all and Logout, always visible when list scrolls"
    - "Clicking + creates doc, switches tab, and opens inline rename without extra steps"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all four Phase 7.1 polish changes on the live application.

Purpose: Visual and interactive behaviors cannot be fully verified by static file scan. This checkpoint confirms the changes look and behave correctly in a real browser before the phase is closed.
Output: User confirms all four behaviors work as intended, or reports issues for gap closure.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-ui-polish-tweaks/07.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to server and confirm automated tests pass</name>
  <files></files>
  <action>
Before asking the user to verify, confirm all automated tests are green and deploy to the server.

1. Run full test suite:
   `cd /c/Users/gmain/Dev/Notes/client && npx vitest run`
   All tests must be GREEN. If any fail, stop and fix before deploying.

2. Deploy changed client files to the server and rebuild:
   ```bash
   scp -r /c/Users/gmain/Dev/Notes/client/src root@192.168.1.50:/root/notes/client/
   scp /c/Users/gmain/Dev/Notes/client/index.css root@192.168.1.50:/root/notes/client/ 2>/dev/null || true
   ssh root@192.168.1.50 "cd /root/notes && docker compose up -d --build"
   ```
   Note: index.css is inside client/src/index.css — it's already covered by the src/ scp above. No separate copy needed.

3. Tail logs briefly to confirm container started:
   `ssh root@192.168.1.50 "cd /root/notes && docker compose logs --tail=20 app"`

Wait for container to be healthy before presenting the verification checklist.
  </action>
  <verify>
    <automated>ssh root@192.168.1.50 "cd /root/notes && docker compose logs --tail=5 app" 2>&1 | grep -v "^$"</automated>
  </verify>
  <done>Container running, no startup errors in logs. App accessible at https://notes.gregorymaingret.fr</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Four UI polish changes deployed to https://notes.gregorymaingret.fr:
1. FocusToolbar: all 11 buttons now show Lucide SVG icons (no more HTML entity glyphs)
2. BulletNode: each bullet row has 2px vertical breathing room
3. Sidebar footer: Export all and Logout appear as persistent rows below the scrollable document list
4. + button: creates a document, switches to Docs tab if needed, and immediately opens inline rename on the new row
  </what-built>
  <how-to-verify>
Open https://notes.gregorymaingret.fr and verify the following:

**1. FocusToolbar icons (tap any bullet to focus it, then look at the toolbar at the bottom of the screen on mobile, or tap a bullet on desktop)**
- Indent / Outdent buttons show clean SVG arrow-tab icons (not "↵" entity chars)
- Move Up / Move Down show clean arrow icons
- Undo / Redo show curved arrow icons
- Attach shows a paperclip
- Note shows a sticky note
- Bookmark shows a bookmark flag
- Complete shows a checkmark
- Delete shows a trash can
Expected: All icons crisp SVG, no browser-rendered Unicode glyphs

**2. Bullet row spacing**
- Open any document with several bullet items
- Each row should have a small but visible gap above and below — feel like Notion's compact density
Expected: 2px breathing room, rows not crammed together

**3. Sidebar footer**
- Open the sidebar
- Scroll the document list down (if you have many docs)
- At the bottom of the sidebar (below the list), you should always see: "Export all" row with upload icon, and "Logout" row with logout icon
- The ... button (three dots) should be gone from the sidebar header
- Hover over Export/Logout rows — they should highlight with a subtle background
Expected: Footer always visible, dropdown gone, hover works

**4. + button inline rename**
- Switch to the Tags tab in the sidebar
- Click the + button (top right of sidebar header)
- Expected sequence: (a) Docs tab activates automatically, (b) a new document row appears, (c) the new row is immediately in rename mode with the cursor in an editable input
- Type a name and press Enter — the document should be renamed
- Click + again while already on Docs tab — same result without tab switch
Expected: Rename activates automatically, no need to open a context menu
  </how-to-verify>
  <resume-signal>
Type "approved" if all four behaviors work correctly.
Or describe any issues found (e.g., "footer not visible", "rename doesn't trigger", "icons look wrong on button X") so gap closure plans can be created.
  </resume-signal>
</task>

</tasks>

<verification>
All automated tests green before deployment:
`cd /c/Users/gmain/Dev/Notes/client && npx vitest run`

User confirms all four behaviors at https://notes.gregorymaingret.fr
</verification>

<success_criteria>
- Automated test suite fully green
- User confirms FocusToolbar icons look correct
- User confirms bullet row spacing feels right
- User confirms sidebar footer is visible and ... dropdown is gone
- User confirms + button triggers inline rename flow correctly
- Phase 7.1 approved and ready for git commit + PR
</success_criteria>

<output>
After user approval, create `.planning/phases/07.1-ui-polish-tweaks/07.1-03-SUMMARY.md`

Then follow the standard workflow (CLAUDE.md):
1. Commit all changes to the phase branch
2. Push to origin
3. Open PR targeting main
4. After CI passes and user merges: sync server from main
</output>
