---
phase: 08-swipe-polish-and-quick-open-palette
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/test/swipePolish.test.ts
  - client/src/test/quickOpenPalette.test.ts
autonomous: true
requirements:
  - GEST-01
  - GEST-02
  - GEST-03
  - GEST-04
  - GEST-05
  - QKOP-01
  - QKOP-02
  - QKOP-03
  - QKOP-04
  - QKOP-05
  - QKOP-06
  - QKOP-07

must_haves:
  truths:
    - "All 12 tests in swipePolish.test.ts and quickOpenPalette.test.ts exist and fail (RED)"
    - "Running vitest on the two test files shows failures for all assertions"
    - "No production code is created or modified in this plan"
  artifacts:
    - path: "client/src/test/swipePolish.test.ts"
      provides: "RED test scaffold for GEST-01..05"
      min_lines: 40
    - path: "client/src/test/quickOpenPalette.test.ts"
      provides: "RED test scaffold for QKOP-01..07"
      min_lines: 50
  key_links:
    - from: "client/src/test/swipePolish.test.ts"
      to: "client/src/components/DocumentView/BulletNode.tsx"
      via: "readFileSync source inspection"
      pattern: "readFileSync.*BulletNode"
    - from: "client/src/test/quickOpenPalette.test.ts"
      to: "client/src/components/DocumentView/QuickOpenPalette.tsx"
      via: "readFileSync source inspection"
      pattern: "readFileSync.*QuickOpenPalette"
---

<objective>
Write failing (RED) test scaffolds for Phase 8 before any production code exists. Two test files cover all 12 Phase 8 requirements: swipe animation polish (GEST-01..05) and quick-open palette (QKOP-01..07).

Purpose: Establish the verification contract that implementation plans must satisfy, following the Wave 0 TDD approach used in phases 5, 6, 7, and 7.1.
Output: Two test files, both fully RED (failing) when this plan completes.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-swipe-polish-and-quick-open-palette/08-CONTEXT.md
@.planning/phases/08-swipe-polish-and-quick-open-palette/08-RESEARCH.md

<interfaces>
<!-- Source inspection test pattern — established in phases 6, 7, 7.1 -->
<!-- Vitest runs with cwd = client/ directory -->

From client/src/test/uiPolish.test.ts (template — follow this exact pattern):
```typescript
import { readFileSync } from 'fs';
import { resolve } from 'path';
import { describe, it, expect } from 'vitest';

const root = process.cwd(); // = client/ at test runtime

const someFilePath = resolve(root, 'src/components/SomeFile.tsx');
const someFile = readFileSync(someFilePath, 'utf-8');

describe('Feature X', () => {
  it('does something', () => {
    expect(someFile).toContain('specificPattern');
  });
});
```

From client/src/test/swipeGesture.test.ts (existing passing tests — do NOT modify):
```typescript
import { describe, it, expect } from 'vitest';
import { swipeThresholdReached } from '../components/DocumentView/gestures';
// Tests swipeThresholdReached pure function — passes today
```

Target files for inspection (do NOT create these yet — they must not exist at Wave 0 time):
- client/src/components/DocumentView/BulletNode.tsx — exists, will be MODIFIED in plan 02
- client/src/components/DocumentView/QuickOpenPalette.tsx — does NOT exist yet (Wave 1 creates it)
- client/src/pages/AppPage.tsx — exists, will be MODIFIED in plan 04
- client/src/store/uiStore.ts — exists, will be MODIFIED in plan 03
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write swipePolish.test.ts — RED scaffold for GEST-01..05</name>
  <files>client/src/test/swipePolish.test.ts</files>
  <behavior>
    - GEST-01: BulletNode.tsx contains icon scale calculation using swipeX ratio (pattern: `ratio` or `iconScale` variable derived from `swipeX`)
    - GEST-02: BulletNode.tsx contains scale for the trash icon (same pattern, left-swipe direction, e.g. `swipeX < 0`)
    - GEST-03: BulletNode.tsx transition string includes `ease` for snap-back (pattern: `'transform 0.2s ease'` — already exists and will stay)
    - GEST-04: BulletNode.tsx contains `exitDirection` state and `onTransitionEnd` handler (pattern: `exitDirection` + `onTransitionEnd`)
    - GEST-05: BulletNode.tsx contains TouchSensor with delay 250 (pattern: `delay: 250` inside useSensors — already exists, test asserts it remains)
  </behavior>
  <action>
    Create `client/src/test/swipePolish.test.ts` using the `readFileSync` source-inspection pattern from `uiPolish.test.ts`.

    Read BulletNode.tsx via `readFileSync` and write assertions that will FAIL now (no icon scale / no exitDirection) but PASS after plan 02 implements them.

    File structure:
    ```
    import { readFileSync } from 'fs';
    import { resolve } from 'path';
    import { describe, it, expect } from 'vitest';

    const root = process.cwd();
    const bulletNodePath = resolve(root, 'src/components/DocumentView/BulletNode.tsx');
    const bulletNode = readFileSync(bulletNodePath, 'utf-8');

    describe('Phase 8 — Swipe animations: icon scale proportional to drag', () => {
      // GEST-01
      it('BulletNode contains iconScale derived from swipeX ratio', () => {
        expect(bulletNode).toMatch(/iconScale/);
      });
      // GEST-02
      it('BulletNode applies iconScale to both Check and Trash2 icon containers', () => {
        expect(bulletNode).toMatch(/scale\(\$\{iconScale\}/);
      });
    });

    describe('Phase 8 — Swipe animations: snap-back and exit', () => {
      // GEST-03: existing transition already present — assert it stays
      it('BulletNode transition includes ease for snap-back', () => {
        expect(bulletNode).toContain('transform 0.2s ease');
      });
      // GEST-04
      it('BulletNode contains exitDirection state', () => {
        expect(bulletNode).toMatch(/exitDirection/);
      });
      it('BulletNode has onTransitionEnd handler that fires mutation', () => {
        expect(bulletNode).toMatch(/onTransitionEnd/);
      });
    });

    describe('Phase 8 — Swipe: dnd-kit sensor delay unchanged', () => {
      // GEST-05: already implemented in Phase 5 — assert it stays
      it('BulletNode uses TouchSensor with delay 250ms', () => {
        expect(bulletNode).toContain('delay: 250');
      });
    });
    ```

    GEST-03 and GEST-05 assertions will PASS today (existing code). GEST-01, GEST-02, GEST-04 assertions will FAIL today (RED) — that is correct and expected for Wave 0.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/dev/Notes/client && npx vitest run src/test/swipePolish.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>File exists. At least 3 tests fail (GEST-01: iconScale, GEST-02: scale transform, GEST-04: exitDirection). GEST-03 and GEST-05 may pass — that is correct since those patterns already exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write quickOpenPalette.test.ts — RED scaffold for QKOP-01..07</name>
  <files>client/src/test/quickOpenPalette.test.ts</files>
  <behavior>
    - QKOP-01: uiStore.ts contains `quickOpenOpen` boolean and `setQuickOpenOpen` action; AppPage.tsx listens for Ctrl+K
    - QKOP-02: QuickOpenPalette.tsx contains logic for recent documents (pattern: `lastOpenedAt` + sort/slice)
    - QKOP-03: QuickOpenPalette.tsx performs client-side substring match on document titles (pattern: `toLowerCase().includes`)
    - QKOP-04: QuickOpenPalette.tsx uses `useSearch` with ≥2 char gate (pattern: `query.length >= 2` or `useSearch`)
    - QKOP-05: QuickOpenPalette.tsx uses `useBookmarks` (pattern: `useBookmarks`)
    - QKOP-06: QuickOpenPalette.tsx handles ArrowDown, ArrowUp, Enter keys for navigation (pattern: `ArrowDown`, `ArrowUp`, `Enter`)
    - QKOP-07: QuickOpenPalette.tsx handles Escape key and backdrop click (pattern: `Escape`, `onClose`)
  </behavior>
  <action>
    Create `client/src/test/quickOpenPalette.test.ts` using the `readFileSync` source-inspection pattern.

    `QuickOpenPalette.tsx` does NOT exist yet — the `readFileSync` on it will throw at test runtime, causing all tests in that describe block to fail RED. Use a try/catch or `existsSync` guard to read the file, falling back to empty string so assertions fail cleanly rather than throwing.

    For `uiStore.ts` and `AppPage.tsx` (which exist today but don't have quickOpen yet), read them and assert the new patterns.

    ```typescript
    import { readFileSync, existsSync } from 'fs';
    import { resolve } from 'path';
    import { describe, it, expect } from 'vitest';

    const root = process.cwd();

    const uiStorePath = resolve(root, 'src/store/uiStore.ts');
    const appPagePath = resolve(root, 'src/pages/AppPage.tsx');
    const palettePath = resolve(root, 'src/components/DocumentView/QuickOpenPalette.tsx');

    const uiStore = readFileSync(uiStorePath, 'utf-8');
    const appPage = readFileSync(appPagePath, 'utf-8');
    const palette = existsSync(palettePath) ? readFileSync(palettePath, 'utf-8') : '';

    describe('Phase 8 — QKOP-01: palette state and Ctrl+K listener', () => {
      it('uiStore contains quickOpenOpen boolean', () => {
        expect(uiStore).toContain('quickOpenOpen');
      });
      it('uiStore contains setQuickOpenOpen action', () => {
        expect(uiStore).toContain('setQuickOpenOpen');
      });
      it('AppPage.tsx listens for Ctrl+K / Cmd+K', () => {
        expect(appPage).toContain("key === 'k'");
      });
    });

    describe('Phase 8 — QKOP-02: recent documents empty state', () => {
      it('QuickOpenPalette sorts by lastOpenedAt', () => {
        expect(palette).toContain('lastOpenedAt');
      });
    });

    describe('Phase 8 — QKOP-03: client-side document title search', () => {
      it('QuickOpenPalette uses case-insensitive substring match', () => {
        expect(palette).toMatch(/toLowerCase\(\)\.includes/);
      });
    });

    describe('Phase 8 — QKOP-04: bullet content search via useSearch', () => {
      it('QuickOpenPalette uses useSearch hook', () => {
        expect(palette).toContain('useSearch');
      });
    });

    describe('Phase 8 — QKOP-05: bookmarks in palette results', () => {
      it('QuickOpenPalette uses useBookmarks hook', () => {
        expect(palette).toContain('useBookmarks');
      });
    });

    describe('Phase 8 — QKOP-06: keyboard navigation', () => {
      it('QuickOpenPalette handles ArrowDown key', () => {
        expect(palette).toContain('ArrowDown');
      });
      it('QuickOpenPalette handles ArrowUp key', () => {
        expect(palette).toContain('ArrowUp');
      });
      it('QuickOpenPalette handles Enter key to open result', () => {
        expect(palette).toContain('Enter');
      });
    });

    describe('Phase 8 — QKOP-07: close on Escape and click-outside', () => {
      it('QuickOpenPalette handles Escape key', () => {
        expect(palette).toContain('Escape');
      });
      it('QuickOpenPalette has backdrop with onClose click handler', () => {
        expect(palette).toContain('onClose');
      });
    });
    ```

    All assertions about `palette` will fail RED since `QuickOpenPalette.tsx` does not exist yet (empty string fallback). Assertions about `uiStore` and `appPage` will also fail RED since the quickOpen patterns haven't been added yet.
  </action>
  <verify>
    <automated>cd /c/Users/gmain/dev/Notes/client && npx vitest run src/test/quickOpenPalette.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>File exists. All 10 assertions fail RED — quickOpenOpen not in uiStore, QuickOpenPalette.tsx does not exist, AppPage has no Ctrl+K handler yet.</done>
</task>

</tasks>

<verification>
Run both test files together and confirm:
1. `swipePolish.test.ts`: GEST-03 (ease transition) and GEST-05 (delay 250) may pass (already in BulletNode). GEST-01, GEST-02, GEST-04 must fail.
2. `quickOpenPalette.test.ts`: All 10 assertions must fail.
3. Existing test suite unaffected: `cd /c/Users/gmain/dev/Notes/client && npx vitest run 2>&1 | tail -10` — only new failures added, no regressions.

```bash
cd /c/Users/gmain/dev/Notes/client && npx vitest run src/test/swipePolish.test.ts src/test/quickOpenPalette.test.ts --reporter=verbose 2>&1 | tail -40
```
</verification>

<success_criteria>
- `swipePolish.test.ts` exists with 6 test assertions covering GEST-01 through GEST-05
- `quickOpenPalette.test.ts` exists with 10 test assertions covering QKOP-01 through QKOP-07
- At least 3 swipe tests and all 10 palette tests are RED (failing)
- No modifications to any production source file
- Full vitest run passes all pre-existing tests (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/08-swipe-polish-and-quick-open-palette/08-00-SUMMARY.md`
</output>
