---
phase: 09-android-foundation-and-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/build.gradle.kts
  - android/settings.gradle.kts
  - android/gradle.properties
  - android/gradle/libs.versions.toml
  - android/.gitignore
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/java/com/gmaingret/notes/NotesApplication.kt
  - android/app/src/main/java/com/gmaingret/notes/MainActivity.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/theme/Color.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/theme/Theme.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/theme/Type.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/model/User.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/model/AuthState.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/repository/AuthRepository.kt
  - android/app/src/main/java/com/gmaingret/notes/data/api/AuthApi.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/AuthResponse.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/RefreshResponse.kt
  - android/app/src/main/res/values/themes.xml
  - android/app/src/main/res/values/colors.xml
  - android/app/src/main/res/drawable/ic_launcher_foreground.xml
  - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - android/app/proguard-rules.pro
autonomous: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "Android project compiles with ./gradlew assembleDebug"
    - "All locked library versions match STATE.md decisions exactly"
    - "Package structure follows Clean Architecture layers (data/domain/presentation/di)"
    - "Material 3 theme uses seed color #2563EB with system dark mode"
  artifacts:
    - path: "android/gradle/libs.versions.toml"
      provides: "Version catalog with all locked versions"
      contains: "okhttp = \"4.12.0\""
    - path: "android/app/build.gradle.kts"
      provides: "App module build config with Hilt, Compose, KSP"
      contains: "com.gmaingret.notes"
    - path: "android/app/src/main/java/com/gmaingret/notes/domain/repository/AuthRepository.kt"
      provides: "Repository interface contract for auth operations"
    - path: "android/app/src/main/java/com/gmaingret/notes/data/api/AuthApi.kt"
      provides: "Retrofit service interface for backend auth API"
  key_links:
    - from: "android/app/build.gradle.kts"
      to: "android/gradle/libs.versions.toml"
      via: "libs.* references"
      pattern: "libs\\."
---

<objective>
Scaffold the Android project with Gradle build system, version catalog, Hilt DI, Material 3 theme, and all domain/data contracts (interfaces + data classes) that downstream plans implement against.

Purpose: Establishes the compilable project skeleton and all type contracts so Plan 03 (data layer) and Plan 04 (presentation) can build against stable interfaces without exploration.
Output: Compilable Android project in /android/ with all dependencies resolved, theme rendering, and typed contracts for auth operations.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-android-foundation-and-auth/09-CONTEXT.md
@.planning/phases/09-android-foundation-and-auth/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Android project scaffold with Gradle and version catalog</name>
  <files>
    android/build.gradle.kts,
    android/settings.gradle.kts,
    android/gradle.properties,
    android/gradle/libs.versions.toml,
    android/.gitignore,
    android/app/build.gradle.kts,
    android/app/proguard-rules.pro,
    android/app/src/main/AndroidManifest.xml,
    android/app/src/main/java/com/gmaingret/notes/NotesApplication.kt,
    android/app/src/main/java/com/gmaingret/notes/MainActivity.kt
  </files>
  <action>
Create the Android project from scratch in the /android/ subdirectory (per locked decision — prevents Gradle scanning node_modules).

**gradle/libs.versions.toml** — Version catalog with ALL locked versions from STATE.md:
- AGP 8.9.0, Kotlin 2.1.20, Gradle 8.13 (wrapper), Hilt 2.59.2, Retrofit 3.0.0, OkHttp 4.12.0, Navigation3 1.0.1, DataStore 1.2.1, Tink 1.8.0, Credential Manager 1.6.0-rc02, core-splashscreen 1.0.1, MockK 1.13.14, Robolectric 4.16, Compose BOM 2025.02.00, KSP 2.1.20-1.0.31, kotlinx-serialization, Gson converter 3.0.0.
- CRITICAL: OkHttp MUST be 4.12.0 — do NOT use 5.x (Retrofit 3 incompatible).
- Use exact version catalog format from 09-RESEARCH.md "Version Catalog" code example.

**build.gradle.kts (root)** — Apply AGP, Kotlin, Hilt, KSP, kotlin-serialization plugins with `apply false`.

**settings.gradle.kts** — Include `:app` module, configure pluginManagement and dependencyResolutionManagement with google() and mavenCentral() repositories.

**gradle.properties** — Enable AndroidX, non-transitive R classes, Kotlin code style, JVM target. Add `android.useAndroidX=true`, `kotlin.code.style=official`, `org.gradle.jvmargs=-Xmx2048m`. Also include the `GOOGLE_WEB_CLIENT_ID` property placeholder (read from local.properties at build time).

**app/build.gradle.kts** — Apply android application, kotlin-android, ksp, hilt, kotlin-serialization plugins. Configure:
- namespace = "com.gmaingret.notes"
- compileSdk = 35, minSdk = 26, targetSdk = 35
- buildFeatures { compose = true, buildConfig = true }
- composeOptions with kotlinCompilerExtensionVersion from BOM
- testOptions { unitTests { isIncludeAndroidResources = true } } (for Robolectric)
- Read GOOGLE_WEB_CLIENT_ID from local.properties into BuildConfig: `buildConfigField("String", "GOOGLE_WEB_CLIENT_ID", "\"${localProperties.getProperty("GOOGLE_WEB_CLIENT_ID", "")}\"")` — use a `val localProperties = Properties().apply { ... }` block.
- Dependencies: all libs from version catalog (compose BOM + ui + material3 + activity-compose, hilt, retrofit, okhttp, navigation3, datastore, tink, credentials, splashscreen, coroutines, kotlinx-serialization-json). Test deps: mockk, robolectric, compose-ui-test-junit4, junit.

**AndroidManifest.xml** — INTERNET permission, application with `android:name=".NotesApplication"`, single activity (MainActivity) with launcher intent filter, edge-to-edge theme reference.

**NotesApplication.kt** — `@HiltAndroidApp class NotesApplication : Application()` (empty body).

**MainActivity.kt** — Minimal ComponentActivity with `@AndroidEntryPoint`, `setContent { NotesTheme { } }`. Just enough to compile — splash/nav logic added in Plan 04.

**android/.gitignore** — Standard Android gitignore (build/, .gradle/, *.iml, local.properties, .idea/, captures/, .externalNativeBuild/, .cxx/).

Also create the Gradle wrapper files: download or generate `gradle/wrapper/gradle-wrapper.properties` pointing to Gradle 8.13 distribution URL (`https://services.gradle.org/distributions/gradle-8.13-bin.zip`). Include `gradle/wrapper/gradle-wrapper.jar` and `gradlew`/`gradlew.bat` scripts.
  </action>
  <verify>
    <automated>cd android && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>Project compiles successfully. All version catalog entries match locked versions. BuildConfig.GOOGLE_WEB_CLIENT_ID field exists (empty string default). Gradle wrapper is 8.13.</done>
</task>

<task type="auto">
  <name>Task 2: Create Material 3 theme and app identity resources</name>
  <files>
    android/app/src/main/java/com/gmaingret/notes/presentation/theme/Color.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/theme/Theme.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/theme/Type.kt,
    android/app/src/main/res/values/themes.xml,
    android/app/src/main/res/values/colors.xml,
    android/app/src/main/res/drawable/ic_launcher_foreground.xml,
    android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  </files>
  <action>
**Color.kt** — Define Material 3 color scheme generated from seed color #2563EB (blue-600). Use `lightColorScheme()` and `darkColorScheme()` with primary = Color(0xFF2563EB), appropriate on-primary, containers, surface, etc. Follow Material 3 tonal palette generation principles.

**Theme.kt** — `NotesTheme` composable:
- Use `isSystemInDarkTheme()` to select light/dark scheme (per locked decision: system preference only, no manual toggle)
- Enable edge-to-edge via `enableEdgeToEdge()` in the theme
- Wrap content in `MaterialTheme(colorScheme, typography, content)`

**Type.kt** — Default Material 3 typography (can use `Typography()` defaults — Claude's discretion on exact scale).

**themes.xml** — XML theme for the splash screen: `Theme.Notes.Splash` extending `Theme.SplashScreen` with `windowSplashScreenBackground` = #2563EB and `postSplashScreenTheme` = `@style/Theme.Notes`. Also `Theme.Notes` extending `Theme.Material3.DayNight.NoActionBar` for the base theme (edge-to-edge, no action bar).

**colors.xml** — Define `seed_blue` = #2563EB, plus any XML-needed colors.

**ic_launcher_foreground.xml** — Vector drawable: white notepad outline icon on transparent background (will show on blue adaptive icon background). Simple path — a rectangle with lines representing a notepad.

**ic_launcher.xml** — Adaptive icon definition: `<adaptive-icon>` with `<background android:drawable="@color/seed_blue"/>` and `<foreground android:drawable="@drawable/ic_launcher_foreground"/>`.

Update AndroidManifest.xml to reference `@style/Theme.Notes.Splash` as the application theme and `@mipmap/ic_launcher` as the icon.
  </action>
  <verify>
    <automated>cd android && ./gradlew assembleDebug 2>&1 | tail -3</automated>
  </verify>
  <done>Theme compiles. NotesTheme composable renders light/dark based on system setting. Adaptive icon defined with blue background + white notepad foreground. Splash theme uses #2563EB background.</done>
</task>

<task type="auto">
  <name>Task 3: Define domain contracts and data models (interfaces-first)</name>
  <files>
    android/app/src/main/java/com/gmaingret/notes/domain/model/User.kt,
    android/app/src/main/java/com/gmaingret/notes/domain/model/AuthState.kt,
    android/app/src/main/java/com/gmaingret/notes/domain/repository/AuthRepository.kt,
    android/app/src/main/java/com/gmaingret/notes/data/api/AuthApi.kt,
    android/app/src/main/java/com/gmaingret/notes/data/model/AuthResponse.kt,
    android/app/src/main/java/com/gmaingret/notes/data/model/RefreshResponse.kt
  </files>
  <action>
Define all type contracts that Plan 03 (data layer) and Plan 04 (UI) will implement against.

**domain/model/User.kt:**
```kotlin
data class User(val id: String, val email: String)
```

**domain/model/AuthState.kt:**
```kotlin
enum class AuthState { CHECKING, AUTHENTICATED, UNAUTHENTICATED }
```

**domain/repository/AuthRepository.kt** — Interface contract:
```kotlin
interface AuthRepository {
    suspend fun register(email: String, password: String): Result<User>
    suspend fun login(email: String, password: String): Result<User>
    suspend fun loginWithGoogle(idToken: String): Result<User>
    suspend fun refresh(): Result<String>  // Returns new access token
    suspend fun logout()
    suspend fun getAccessToken(): String?
    suspend fun isLoggedIn(): Boolean
}
```

**data/api/AuthApi.kt** — Retrofit service interface matching existing backend contracts:
```kotlin
interface AuthApi {
    @POST("api/auth/register")
    suspend fun register(@Body request: RegisterRequest): AuthResponse

    @POST("api/auth/login")
    suspend fun login(@Body request: LoginRequest): AuthResponse

    @POST("api/auth/google/token")
    suspend fun googleToken(@Body request: GoogleTokenRequest): AuthResponse

    @POST("api/auth/refresh")
    suspend fun refresh(): RefreshResponse

    @POST("api/auth/logout")
    suspend fun logout()
}
```
Define the request body data classes inline or in separate files: `RegisterRequest(email, password)`, `LoginRequest(email, password)`, `GoogleTokenRequest(idToken)`.

**data/model/AuthResponse.kt:**
```kotlin
data class AuthResponse(val accessToken: String, val user: UserDto)
data class UserDto(val id: String, val email: String)
```

**data/model/RefreshResponse.kt:**
```kotlin
data class RefreshResponse(val accessToken: String)
```

Backend API contracts (verified from server/src/routes/auth.ts):
- POST /api/auth/register: body {email, password} -> 201 {accessToken, user: {id, email}} or 400 {errors} or 409 {field, message}
- POST /api/auth/login: body {email, password} -> 200 {accessToken, user: {id, email}} or 401 {error}
- POST /api/auth/google/token: body {idToken} -> 200 {accessToken, user: {id, email}} or 401 {error}
- POST /api/auth/refresh: (cookie) -> 200 {accessToken} or 401 {error}
- POST /api/auth/logout: -> 200 {message}

All response shapes confirmed from server source code.
  </action>
  <verify>
    <automated>cd android && ./gradlew assembleDebug 2>&1 | tail -3</automated>
  </verify>
  <done>All domain interfaces and data models compile. AuthRepository interface defines the full auth contract. AuthApi Retrofit interface matches backend API shapes exactly. Domain models (User, AuthState) are available for downstream plans.</done>
</task>

</tasks>

<verification>
- `cd android && ./gradlew assembleDebug` completes with BUILD SUCCESSFUL
- Version catalog contains all locked versions (AGP 8.9.0, Kotlin 2.1.20, OkHttp 4.12.0, Retrofit 3.0.0, etc.)
- Package structure has data/api, data/model, data/repository, domain/model, domain/repository, domain/usecase, presentation/auth, presentation/main, presentation/theme, di directories
- NotesTheme composable exists and respects system dark mode
- AuthRepository interface and AuthApi Retrofit interface are defined with correct method signatures
</verification>

<success_criteria>
- Android project compiles from clean state
- All version catalog entries match locked decisions from STATE.md
- Domain contracts (AuthRepository, AuthApi) define the full auth operation surface
- Material 3 theme uses #2563EB seed color with system dark mode support
- Adaptive launcher icon renders blue background with white notepad
</success_criteria>

<output>
After completion, create `.planning/phases/09-android-foundation-and-auth/09-01-SUMMARY.md`
</output>
