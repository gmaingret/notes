---
phase: 11-bullet-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/gmaingret/notes/data/api/BulletApi.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/BulletDto.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/CreateBulletRequest.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/PatchBulletRequest.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/MoveBulletRequest.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/UndoStatusDto.kt
  - android/app/src/main/java/com/gmaingret/notes/data/model/UndoCheckpointRequest.kt
  - android/app/src/main/java/com/gmaingret/notes/data/repository/BulletRepositoryImpl.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/model/Bullet.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/model/FlatBullet.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/model/UndoStatus.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/repository/BulletRepository.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/GetBulletsUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/CreateBulletUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/PatchBulletUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/DeleteBulletUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/IndentBulletUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/OutdentBulletUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/MoveBulletUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/UndoUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/RedoUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/GetUndoStatusUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/domain/usecase/FlattenTreeUseCase.kt
  - android/app/src/main/java/com/gmaingret/notes/di/NetworkModule.kt
  - android/app/src/main/java/com/gmaingret/notes/di/DataModule.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeUiState.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeViewModel.kt
  - android/app/src/test/java/com/gmaingret/notes/domain/usecase/FlattenTreeUseCaseTest.kt
autonomous: true
requirements:
  - TREE-01
  - TREE-07

must_haves:
  truths:
    - "BulletApi can fetch bullets for a document and call all 11 endpoints"
    - "FlattenTreeUseCase converts parent-linked bullets into depth-ordered flat list"
    - "FlattenTreeUseCase respects isCollapsed flag (hides children of collapsed bullets)"
    - "FlattenTreeUseCase supports zoom mode (rootId parameter)"
    - "BulletTreeViewModel loads bullets and exposes Success/Loading/Error states"
    - "Operation queue serializes structural mutations"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/notes/data/api/BulletApi.kt"
      provides: "Retrofit interface for all bullet endpoints"
      exports: ["getBullets", "createBullet", "patchBullet", "deleteBullet", "indentBullet", "outdentBullet", "moveBullet", "undo", "redo", "getUndoStatus", "undoCheckpoint"]
    - path: "android/app/src/main/java/com/gmaingret/notes/domain/usecase/FlattenTreeUseCase.kt"
      provides: "Pure Kotlin recursive DFS tree flattener"
      exports: ["invoke(bullets, rootId?, maxDisplayDepth?)"]
    - path: "android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeViewModel.kt"
      provides: "ViewModel with state, operation queue, load/reload"
      exports: ["uiState", "snackbarMessage", "zoomRootId", "breadcrumbPath", "canUndo", "canRedo", "loadBullets"]
    - path: "android/app/src/test/java/com/gmaingret/notes/domain/usecase/FlattenTreeUseCaseTest.kt"
      provides: "Unit tests for FlattenTreeUseCase"
      min_lines: 80
  key_links:
    - from: "BulletRepositoryImpl"
      to: "BulletApi"
      via: "Retrofit HTTP calls wrapped in Result<T>"
      pattern: "try.*bulletApi\\."
    - from: "BulletTreeViewModel"
      to: "use cases"
      via: "Hilt @Inject constructor"
      pattern: "@HiltViewModel"
    - from: "NetworkModule"
      to: "BulletApi"
      via: "provideBulletApi"
      pattern: "retrofit\\.create.*BulletApi"
---

<objective>
Build the entire bullet tree data layer, domain models, use cases, and ViewModel scaffold with operation queue.

Purpose: Establish all contracts and data flow so that Plan 02 (ViewModel operations + tests) and Plan 03 (UI composables) can build against stable interfaces without needing to create any data/domain code.
Output: BulletApi, BulletRepository, 11 use cases, FlattenTreeUseCase (TDD-tested), BulletTreeViewModel skeleton with load + state + operation queue.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bullet-tree/11-CONTEXT.md
@.planning/phases/11-bullet-tree/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 10 that this plan mirrors -->

From android/.../data/api/DocumentApi.kt:
```kotlin
interface DocumentApi {
    @GET("api/documents")
    suspend fun getDocuments(): List<DocumentDto>
    @POST("api/documents")
    suspend fun createDocument(@Body request: CreateDocumentRequest): DocumentDto
    @DELETE("api/documents/{id}")
    suspend fun deleteDocument(@Path("id") id: String): Response<Unit>
}
```

From android/.../data/repository/DocumentRepositoryImpl.kt pattern:
```kotlin
// All methods wrap API calls in try/catch returning Result<T>
override suspend fun getDocuments(): Result<List<Document>> = try {
    val docs = documentApi.getDocuments().map { it.toDomain() }.sortedBy { it.position }
    Result.success(docs)
} catch (e: Exception) {
    Result.failure(e)
}
```

From android/.../di/NetworkModule.kt:
```kotlin
@Provides @Singleton
fun provideDocumentApi(retrofit: Retrofit): DocumentApi =
    retrofit.create(DocumentApi::class.java)
```

From android/.../di/DataModule.kt:
```kotlin
@Binds @Singleton
abstract fun bindDocumentRepository(impl: DocumentRepositoryImpl): DocumentRepository
```

From android/.../presentation/main/MainUiState.kt:
```kotlin
sealed interface MainUiState {
    data object Loading : MainUiState
    data class Success(...) : MainUiState
    data class Error(val message: String) : MainUiState
    data object Empty : MainUiState
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain models, BulletApi, DTOs, repository, use cases, DI wiring</name>
  <files>
    android/app/src/main/java/com/gmaingret/notes/domain/model/Bullet.kt
    android/app/src/main/java/com/gmaingret/notes/domain/model/FlatBullet.kt
    android/app/src/main/java/com/gmaingret/notes/domain/model/UndoStatus.kt
    android/app/src/main/java/com/gmaingret/notes/domain/repository/BulletRepository.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/GetBulletsUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/CreateBulletUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/PatchBulletUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/DeleteBulletUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/IndentBulletUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/OutdentBulletUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/MoveBulletUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/UndoUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/RedoUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/GetUndoStatusUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/domain/usecase/FlattenTreeUseCase.kt
    android/app/src/main/java/com/gmaingret/notes/data/api/BulletApi.kt
    android/app/src/main/java/com/gmaingret/notes/data/model/BulletDto.kt
    android/app/src/main/java/com/gmaingret/notes/data/model/CreateBulletRequest.kt
    android/app/src/main/java/com/gmaingret/notes/data/model/PatchBulletRequest.kt
    android/app/src/main/java/com/gmaingret/notes/data/model/MoveBulletRequest.kt
    android/app/src/main/java/com/gmaingret/notes/data/model/UndoStatusDto.kt
    android/app/src/main/java/com/gmaingret/notes/data/model/UndoCheckpointRequest.kt
    android/app/src/main/java/com/gmaingret/notes/data/repository/BulletRepositoryImpl.kt
    android/app/src/main/java/com/gmaingret/notes/di/NetworkModule.kt
    android/app/src/main/java/com/gmaingret/notes/di/DataModule.kt
    android/app/src/test/java/com/gmaingret/notes/domain/usecase/FlattenTreeUseCaseTest.kt
  </files>
  <behavior>
    - FlattenTreeUseCase: flat list of [A(depth=0), B(depth=1), C(depth=2)] from parent-linked bullets A->B->C
    - FlattenTreeUseCase: collapsed parent hides all descendants in output
    - FlattenTreeUseCase: rootId=X returns only children of X at depth=0
    - FlattenTreeUseCase: maxDisplayDepth caps visual depth (deep bullets still appear, just at capped depth)
    - FlattenTreeUseCase: empty input returns empty list
    - FlattenTreeUseCase: siblings ordered by position ascending (server ordering preserved)
  </behavior>
  <action>
    **Domain models:**
    - `Bullet.kt`: data class with id, documentId, parentId (String?), content (String), position (Double), isComplete (Boolean), isCollapsed (Boolean), note (String?)
    - `FlatBullet.kt`: data class wrapping Bullet + depth (Int) + hasChildren (Boolean)
    - `UndoStatus.kt`: data class with canUndo (Boolean), canRedo (Boolean)

    **BulletApi.kt** — Retrofit interface with ALL 11 endpoints. Follow DocumentApi pattern exactly:
    - `GET api/bullets/documents/{docId}/bullets` returns `List<BulletDto>`
    - `POST api/bullets` with CreateBulletRequest returns `BulletDto`
    - `PATCH api/bullets/{id}` with PatchBulletRequest returns `BulletDto`
    - `DELETE api/bullets/{id}` returns `Response<Unit>` (server returns 200 with `{ok:true}` but we ignore body — same pattern as DocumentApi.deleteDocument)
    - `POST api/bullets/{id}/indent` returns `BulletDto`
    - `POST api/bullets/{id}/outdent` returns `BulletDto`
    - `POST api/bullets/{id}/move` with MoveBulletRequest returns `BulletDto`
    - `POST api/undo` returns `UndoStatusDto`
    - `POST api/redo` returns `UndoStatusDto`
    - `GET api/undo/status` returns `UndoStatusDto`
    - `POST api/bullets/{id}/undo-checkpoint` with UndoCheckpointRequest returns `Response<Unit>`

    **DTOs:**
    - `BulletDto.kt`: mirrors server Bullet shape with @SerializedName annotations and toDomain() mapper
    - `CreateBulletRequest.kt`: documentId, parentId?, afterId?, content?
    - `PatchBulletRequest.kt`: content?, isComplete?, isCollapsed?, note? — IMPORTANT: server processes ONE field at a time (first non-null wins), so callers must send only the field being changed
    - `MoveBulletRequest.kt`: newParentId (String?), afterId (String?)
    - `UndoStatusDto.kt`: canUndo, canRedo with toDomain()
    - `UndoCheckpointRequest.kt`: content (String), previousContent (String?)

    **BulletRepository interface** — 11 methods all returning Result<T>:
    getBullets(docId), createBullet(req), patchBullet(id, req), deleteBullet(id), indentBullet(id), outdentBullet(id), moveBullet(id, req), undo(), redo(), getUndoStatus(), undoCheckpoint(id, req)

    **BulletRepositoryImpl** — follows DocumentRepositoryImpl try/catch Result wrapping. `@Singleton` with `@Inject constructor(private val bulletApi: BulletApi)`.

    **Use cases** — each is a single-method class with `@Inject constructor` and `operator fun invoke()` delegating to BulletRepository. Follow existing use case pattern (GetDocumentsUseCase, etc.).

    **FlattenTreeUseCase** — pure Kotlin, NO Android deps, NO @Inject (instantiated directly in tests):
    ```
    operator fun invoke(bullets: List<Bullet>, rootId: String? = null, maxDisplayDepth: Int = 7): List<FlatBullet>
    ```
    Implementation: groupBy parentId, recursive DFS from rootId (null = document root). Skip children of collapsed bullets. Compute hasChildren from childMap. Cap depth at maxDisplayDepth for display.

    **DI wiring:**
    - NetworkModule: add `provideBulletApi(retrofit: Retrofit): BulletApi`
    - DataModule: add `bindBulletRepository(impl: BulletRepositoryImpl): BulletRepository`

    **FlattenTreeUseCaseTest** — TDD: write tests FIRST, then implement FlattenTreeUseCase:
    - Test flat list (no nesting): 3 root bullets ordered by position
    - Test nested: parent with 2 children produces [parent(0), child1(1), child2(1)]
    - Test deep nesting: grandchild at depth 2
    - Test collapsed parent: children hidden
    - Test zoom mode: rootId filters to subtree with depth starting at 0
    - Test maxDisplayDepth: bullets deeper than cap appear at capped depth
    - Test empty input
    - Test sibling ordering by position
  </action>
  <verify>
    <automated>cd C:/Users/gmain/Dev/Notes/android && ./gradlew testDebugUnitTest --tests "*.FlattenTreeUseCaseTest" -q 2>&1 | tail -20</automated>
  </verify>
  <done>All domain models, BulletApi, DTOs, repository, 11 use cases, and FlattenTreeUseCase exist. FlattenTreeUseCaseTest passes with 8+ test cases covering nesting, collapse, zoom, and depth cap.</done>
</task>

<task type="auto">
  <name>Task 2: BulletTreeViewModel scaffold with load, state, and operation queue</name>
  <files>
    android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeUiState.kt
    android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeViewModel.kt
  </files>
  <action>
    **BulletTreeUiState.kt** — sealed interface following MainUiState pattern:
    ```kotlin
    sealed interface BulletTreeUiState {
        data object Loading : BulletTreeUiState
        data class Success(
            val bullets: List<Bullet>,      // raw server bullets (for optimistic updates)
            val flatList: List<FlatBullet>,  // flattened for LazyColumn
            val focusedBulletId: String? = null,
            val focusCursorEnd: Boolean = false  // true = place cursor at end (after backspace merge)
        ) : BulletTreeUiState
        data class Error(val message: String) : BulletTreeUiState
    }
    ```

    **BulletTreeViewModel.kt** — @HiltViewModel with @Inject constructor taking ALL use cases + FlattenTreeUseCase.

    **State flows:**
    - `_uiState: MutableStateFlow<BulletTreeUiState>` (Loading initially)
    - `_snackbarMessage: MutableSharedFlow<String>`
    - `_zoomRootId: MutableStateFlow<String?>(null)`
    - `_breadcrumbPath: MutableStateFlow<List<Bullet>>(emptyList())`
    - `_canUndo: MutableStateFlow<Boolean>(false)`
    - `_canRedo: MutableStateFlow<Boolean>(false)`
    - All exposed as public StateFlow/SharedFlow via asStateFlow()/asSharedFlow()

    **Operation queue:**
    ```kotlin
    private val operationQueue = Channel<suspend () -> Unit>(capacity = Channel.UNLIMITED)
    init {
        viewModelScope.launch {
            for (op in operationQueue) { op() }
        }
    }
    private fun enqueue(op: suspend () -> Unit) { operationQueue.trySend(op) }
    ```

    **Core methods (implement fully):**
    - `fun loadBullets(documentId: String)` — sets Loading, calls getBulletsUseCase, on success calls updateState + loadUndoStatus, on failure sets Error state. Stores documentId in a private var for reload.
    - `private fun updateState(bullets: List<Bullet>)` — runs flattenTreeUseCase(bullets, rootId=zoomRootId.value), emits Success state. Also computes breadcrumbPath by walking up parentId chain from zoomRootId.
    - `private fun reloadFromServer()` — calls loadBullets with stored documentId (no Loading state — just silently refresh). Used after operation failures and undo/redo.
    - `private fun loadUndoStatus()` — calls getUndoStatusUseCase, updates _canUndo/_canRedo.

    **Stub methods (signature + TODO body — Plan 02 implements):**
    - `fun createBullet(afterBulletId: String?, parentId: String?)`
    - `fun deleteBullet(bulletId: String)`
    - `fun indentBullet(bulletId: String)`
    - `fun outdentBullet(bulletId: String)`
    - `fun moveUp(bulletId: String)`
    - `fun moveDown(bulletId: String)`
    - `fun toggleCollapse(bulletId: String)`
    - `fun updateContent(bulletId: String, content: String)` (debounced save)
    - `fun saveNote(bulletId: String, note: String)` (debounced save)
    - `fun undo()`
    - `fun redo()`
    - `fun setFocusedBullet(bulletId: String?)`
    - `fun zoomTo(bulletId: String?)`
    - `fun enterOnEmpty(bulletId: String)` — Enter on empty bullet (outdent or unfocus at root)
    - `fun backspaceOnEmpty(bulletId: String)` — Backspace on empty bullet (delete + merge)
    - `fun moveBulletLocally(fromIndex: Int, toIndex: Int)` — optimistic drag reorder
    - `fun commitBulletMove(bulletId: String, newParentId: String?, afterId: String?)` — fire API after drag

    Verify project compiles: `./gradlew compileDebugKotlin`
  </action>
  <verify>
    <automated>cd C:/Users/gmain/Dev/Notes/android && ./gradlew compileDebugKotlin -q 2>&1 | tail -10</automated>
  </verify>
  <done>BulletTreeUiState sealed interface and BulletTreeViewModel exist with load/state/queue working. All operation method signatures stubbed. Project compiles cleanly.</done>
</task>

</tasks>

<verification>
- FlattenTreeUseCaseTest passes: `cd android && ./gradlew testDebugUnitTest --tests "*.FlattenTreeUseCaseTest"`
- Full compile succeeds: `cd android && ./gradlew compileDebugKotlin`
- All 25+ source files created in correct package directories
</verification>

<success_criteria>
- BulletApi Retrofit interface with 11 endpoints compiles
- BulletRepository with Result<T> wrapping for all operations
- FlattenTreeUseCase passes 8+ unit tests (nesting, collapse, zoom, depth cap)
- BulletTreeViewModel loads bullets and emits Success state with flat list
- Operation queue initialized and consuming
- DI wiring (NetworkModule + DataModule) complete
- Full project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-bullet-tree/11-01-SUMMARY.md`
</output>
