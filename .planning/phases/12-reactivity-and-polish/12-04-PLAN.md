---
phase: 12-reactivity-and-polish
plan: "04"
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeScreen.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/main/DocumentDrawerContent.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/main/MainViewModel.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/theme/Theme.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/theme/Type.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletRow.kt
  - android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletEditingToolbar.kt
autonomous: true
requirements:
  - POLL-01
  - POLL-02
  - POLL-06
  - POLL-07
  - POLL-08

must_haves:
  truths:
    - "Pull-to-refresh works on bullet tree view"
    - "Pull-to-refresh works on document drawer list"
    - "All screens show loading state on first load"
    - "Error states show retry button that recovers"
    - "Dark theme activates based on system preference"
    - "Undo/redo toolbar buttons are functional"
    - "AnimatedVisibility and animateItem are used in LazyColumn"
    - "UI elements are sized for comfortable touch targets"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeScreen.kt"
      provides: "PullToRefreshBox wrapper, enhanced animations"
      contains: "PullToRefreshBox"
    - path: "android/app/src/main/java/com/gmaingret/notes/presentation/theme/Theme.kt"
      provides: "Dark theme with system preference detection"
      contains: "isSystemInDarkTheme"
    - path: "android/app/src/main/java/com/gmaingret/notes/presentation/theme/Type.kt"
      provides: "Slightly larger typography for better readability"
      contains: "Typography"
  key_links:
    - from: "BulletTreeScreen"
      to: "BulletTreeViewModel.refresh()"
      via: "PullToRefreshBox onRefresh callback"
      pattern: "PullToRefreshBox"
    - from: "Theme.kt"
      to: "DarkColorScheme"
      via: "isSystemInDarkTheme()"
      pattern: "darkColorScheme"
---

<objective>
Add pull-to-refresh, verify loading/error states, dark theme, animation polish, undo/redo verification, and UI size adjustments.

Purpose: These are the cross-cutting polish requirements that make every screen feel complete. Pull-to-refresh adds manual refresh capability, dark theme respects system preference, animations add polish, and UI size tweaks improve touch usability. POLL-06 (undo/redo) and POLL-07 (dark theme) may already be partially satisfied from Phase 11 — this plan verifies and completes them.

Output: PullToRefreshBox on bullet tree and document drawer, dark theme verification/completion, animation enhancements, undo/redo verification, UI size adjustments.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-reactivity-and-polish/12-RESEARCH.md
@.planning/phases/12-reactivity-and-polish/12-02-SUMMARY.md
@.planning/phases/12-reactivity-and-polish/12-03-SUMMARY.md

<interfaces>
<!-- From Phase 11 and Plan 02 -->

From presentation/bullet/BulletTreeViewModel.kt (after Plan 02):
```kotlin
val isRefreshing: StateFlow<Boolean>
fun refresh() // reloads bullets from server, sets _isRefreshing
```

From presentation/main/MainViewModel.kt:
```kotlin
fun refreshDocuments() // already exists, reloads document list
// Need to add: isRefreshing StateFlow for drawer pull-to-refresh
```

From presentation/theme/Theme.kt (existing):
```kotlin
// Already has DarkColorScheme, LightColorScheme, and isSystemInDarkTheme() usage
// Verify it's correctly wired
```

From presentation/theme/Type.kt (existing):
```kotlin
// Current Typography definitions — need slight size increase
```

From presentation/bullet/BulletTreeScreen.kt (existing):
```kotlin
// Already uses AnimatedVisibility for breadcrumb/toolbar
// Already uses animateItem on LazyColumn items
// Need to add PullToRefreshBox wrapper
```

From presentation/bullet/BulletEditingToolbar.kt:
```kotlin
// Already has undo/redo buttons wired to ViewModel
// Verify buttons are enabled/disabled based on undoStatus
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PullToRefreshBox to BulletTreeScreen and DocumentDrawerContent</name>
  <files>
    android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeScreen.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/main/DocumentDrawerContent.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/main/MainViewModel.kt
  </files>
  <action>
1. In `BulletTreeScreen.kt`:
   - Import `PullToRefreshBox` from `androidx.compose.material3`
   - Collect `val isRefreshing by viewModel.isRefreshing.collectAsState()`
   - Wrap the existing LazyColumn (the one inside the Success state) with `PullToRefreshBox(isRefreshing = isRefreshing, onRefresh = { viewModel.refresh() })`. The PullToRefreshBox must be INSIDE the `imePadding` Column, not outside it (per research pitfall #4 — prevents visual glitch when keyboard is open).
   - Add `@OptIn(ExperimentalMaterial3Api::class)` if not already present (PullToRefreshBox is experimental in BOM 2025.02)

2. In `MainViewModel.kt`:
   - Add `private val _isRefreshing = MutableStateFlow(false)` and expose as `val isRefreshing: StateFlow<Boolean>`
   - Modify `refreshDocuments()` to set `_isRefreshing.value = true` at start and `_isRefreshing.value = false` at end (in both success and failure paths)

3. In `DocumentDrawerContent.kt`:
   - Import `PullToRefreshBox`
   - Accept new parameters: `isRefreshing: Boolean`, `onRefresh: () -> Unit`
   - Wrap the document LazyColumn with `PullToRefreshBox(isRefreshing = isRefreshing, onRefresh = onRefresh)`
   - In MainScreen.kt where DocumentDrawerContent is called, pass `isRefreshing = mainViewModel.isRefreshing.collectAsState().value` and `onRefresh = { mainViewModel.refreshDocuments() }`

4. Verify loading/error states already exist on all screens:
   - BulletTreeScreen: Loading (shimmer) -> exists from Phase 11
   - BulletTreeScreen: Error (retry) -> exists from Phase 11
   - MainScreen: Loading, Error, Empty -> all exist from Phase 10
   - BookmarksScreen: Loading, Error, Empty -> created in Plan 03
   - If any screen is missing loading/error states, add them following the established pattern (Loading = shimmer or CircularProgressIndicator, Error = message + Retry button)
  </action>
  <verify>
    <automated>cd C:/Users/gmain/Dev/Notes/android && ./gradlew :app:assembleDebug -x lint 2>&1 | tail -5</automated>
  </verify>
  <done>PullToRefreshBox wraps both bullet tree and document drawer LazyColumns. Pull-down gesture triggers refresh. Loading/error states verified on all screens.</done>
</task>

<task type="auto">
  <name>Task 2: Dark theme verification, animation polish, undo/redo check, UI size increase</name>
  <files>
    android/app/src/main/java/com/gmaingret/notes/presentation/theme/Theme.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/theme/Type.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletRow.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletEditingToolbar.kt,
    android/app/src/main/java/com/gmaingret/notes/presentation/bullet/BulletTreeScreen.kt
  </files>
  <action>
1. **Dark theme verification and completion** — Read `Theme.kt` and `Color.kt`:
   - Verify `DarkColorScheme` exists with colors derived from seed #2563EB (blue)
   - Verify `NotesTheme` composable uses `isSystemInDarkTheme()` to select between LightColorScheme and DarkColorScheme
   - Verify all screens use `MaterialTheme.colorScheme` (not hardcoded colors)
   - If DarkColorScheme is missing or incomplete, add it using `darkColorScheme()` with appropriate dark variants of the seed color
   - Check guide line colors in BulletRow `drawBehind` — ensure they use `MaterialTheme.colorScheme.outlineVariant` (not hardcoded gray) so they adapt to dark mode
   - Check shimmer colors — ensure they use theme-aware colors

2. **Undo/redo verification** — Read `BulletEditingToolbar.kt` and `BulletTreeViewModel.kt`:
   - Verify undo button calls `viewModel.undo()` and redo button calls `viewModel.redo()`
   - Verify buttons are enabled/disabled based on `undoStatus.canUndo` and `undoStatus.canRedo`
   - If any wiring is missing, fix it. If already wired (expected from Phase 11), document that POLL-06 is satisfied.

3. **Animation enhancements** — Review and enhance:
   - Verify `animateItem()` modifier is present on LazyColumn items in BulletTreeScreen (for smooth item reorder and insertion/removal animations)
   - Add `Crossfade` transition for the MainScreen content area switching between BulletTreeScreen/BookmarksScreen/Empty states:
     ```kotlin
     Crossfade(targetState = contentKey, label = "content-crossfade") { key ->
         when (key) { /* render appropriate screen */ }
     }
     ```
   - Verify `AnimatedVisibility` is used for breadcrumb row, toolbar entrance, and note field expansion
   - Add `animateContentSize()` modifier on BulletRow's outer Column if not present — smoothly animates when attachment list or note field expands/collapses

4. **UI size increase** — Per user decision "increase overall UI size slightly":
   - In `Type.kt`: Increase base font sizes by ~1-2sp across the board:
     - bodyLarge: 16sp -> 17sp (bullet content)
     - bodyMedium: 14sp -> 15sp (secondary text)
     - bodySmall: 12sp -> 13sp (labels, chips)
     - titleLarge: 22sp -> 24sp (app bar)
     - titleMedium: 16sp -> 18sp (section headers)
   - In `BulletRow.kt`: Increase minimum touch target height from any current value to 48.dp minimum (Material 3 recommended). Increase bullet icon size from current to 10.dp if smaller. Increase horizontal padding slightly (e.g., indent per level from 20.dp to 24.dp if currently less).
   - In `BulletEditingToolbar.kt`: Ensure toolbar button touch targets are at least 48.dp. If icon buttons are smaller, add `Modifier.size(48.dp)`.
   - In `DocumentDrawerContent.kt` (if modifying): Ensure document row height is at least 56.dp for comfortable touch.
  </action>
  <verify>
    <automated>cd C:/Users/gmain/Dev/Notes/android && ./gradlew :app:testDebugUnitTest -x lint 2>&1 | tail -5</automated>
  </verify>
  <done>Dark theme works with system preference. Undo/redo confirmed functional. Animations applied (Crossfade, animateItem, AnimatedVisibility, animateContentSize). UI text and touch targets increased. All tests pass.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug -x lint` passes
- `./gradlew :app:testDebugUnitTest -x lint` passes
- Pull-to-refresh works on bullet tree and document drawer
- Dark theme activates when system is in dark mode
- Undo/redo buttons respond correctly
- Animations are smooth (Crossfade, AnimatedVisibility, animateItem)
- Text and touch targets are visibly larger
</verification>

<success_criteria>
- PullToRefreshBox on bullet tree view and document drawer
- Loading/error/retry states on all screens (BulletTree, Main, Bookmarks)
- Dark theme toggles with system preference, all UI elements adapt
- Undo/redo toolbar buttons functional and enabled/disabled correctly
- AnimatedVisibility, animateItem, Crossfade used appropriately
- UI text sizes increased by ~1-2sp, touch targets at least 48dp
</success_criteria>

<output>
After completion, create `.planning/phases/12-reactivity-and-polish/12-04-SUMMARY.md`
</output>
