---
phase: 12-reactivity-and-polish
plan: "05"
type: execute
wave: 3
depends_on: ["12-02", "12-03", "12-04"]
files_modified: []
autonomous: false
requirements:
  - CONT-03
  - CONT-04
  - POLL-01
  - POLL-02
  - POLL-03
  - POLL-04
  - POLL-05
  - POLL-06
  - POLL-07
  - POLL-08

must_haves:
  truths:
    - "All Phase 12 features work end-to-end on the production server"
    - "Swipe, search, bookmarks, attachments, pull-to-refresh, dark theme all functional"
  artifacts: []
  key_links: []
---

<objective>
Build, deploy, and verify all Phase 12 features end-to-end on the production server.

Purpose: Final verification that all features work on real hardware against the production API before committing and merging.

Output: Debug APK deployed to device, all features verified by user.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-reactivity-and-polish/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build APK and install on device</name>
  <files></files>
  <action>
1. Run full test suite: `cd android && ./gradlew :app:testDebugUnitTest -x lint`
2. Build debug APK: `cd android && ./gradlew :app:assembleDebug -x lint`
3. Install on connected device: `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`
  </action>
  <verify>
    <automated>cd C:/Users/gmain/Dev/Notes/android && ./gradlew :app:assembleDebug -x lint 2>&1 | tail -3</automated>
  </verify>
  <done>Debug APK built and installed on device.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 12: swipe gestures, search, bookmarks, attachments, pull-to-refresh, dark theme, animations, UI size increase</what-built>
  <how-to-verify>
    1. **Swipe gestures**: Open a document with bullets. Swipe right on a bullet — should show green background + checkmark proportionally, completing the bullet with strikethrough. Swipe right again to un-complete. Swipe left — should show red background + trash, deleting the bullet. Verify haptic feedback on threshold.
    2. **Search**: Tap the search icon in the top bar. Type a query — results should appear after a brief debounce, grouped by document. Tap a result — should navigate to that document and scroll to the bullet with a brief highlight.
    3. **Bookmarks**: Long-press a bullet to see context menu. Tap "Bookmark". Open drawer — "Bookmarks" should appear above documents. Tap it — bookmarks screen shows the bookmarked bullet. Tap the bookmark to navigate to it.
    4. **Attachments**: Long-press a bullet that has attachments. Tap "Attachments" — inline list should expand below the bullet. Images show as thumbnails. Tap an attachment to download.
    5. **Pull-to-refresh**: Pull down on the bullet tree — refresh indicator should appear and data should reload. Also try in the document drawer.
    6. **Dark theme**: Switch phone to dark mode in system settings. App should switch to dark theme automatically.
    7. **Undo/redo**: Make a change (e.g., complete a bullet), tap undo in toolbar — should revert.
    8. **UI size**: Overall text and touch targets should be noticeably larger/more comfortable than before.
    9. **Chip search**: In a bullet with a #tag, tap the tag chip when not editing — should open search pre-filled with that tag.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All Phase 12 requirements verified on physical device against production server.
</verification>

<success_criteria>
User confirms all features work correctly on device.
</success_criteria>

<output>
After completion, create `.planning/phases/12-reactivity-and-polish/12-05-SUMMARY.md`
</output>
