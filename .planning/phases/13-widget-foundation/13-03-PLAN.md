---
phase: 13-widget-foundation
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - android/app/src/main/java/com/gmaingret/notes/widget/WidgetContent.kt
  - android/app/src/main/java/com/gmaingret/notes/widget/RetryActionCallback.kt
  - android/app/src/main/java/com/gmaingret/notes/widget/ReconfigureActionCallback.kt
  - android/app/src/main/java/com/gmaingret/notes/widget/NotesWidget.kt
  - android/app/src/test/java/com/gmaingret/notes/widget/WidgetContentHelperTest.kt
autonomous: false
requirements: [DISP-01, DISP-02, DISP-03]

must_haves:
  truths:
    - "Widget header shows the selected document's title left-aligned with ellipsis truncation, plus a [+] add button placeholder on the right"
    - "Widget body shows root-level bullets as a scrollable flat list with bullet dot + text"
    - "Completed bullets show strikethrough and reduced-opacity color"
    - "Widget shows centered 'No bullets yet' text when document has no bullets"
    - "Widget shows 3-4 static gray placeholder rows when loading"
    - "Widget shows 'Couldn't load - tap to retry' when in error state; tapping triggers re-render"
    - "Widget shows 'Document not found' with tap-to-reconfigure when document is deleted"
    - "Widget shows 'Session expired' with tap-to-login when auth fails"
    - "Tapping the document title opens the full Notes app to that document"
    - "Tapping a bullet row opens the full Notes app to the document"
    - "Widget follows system dark/light mode via GlanceTheme"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/notes/widget/WidgetContent.kt"
      provides: "All Glance composables: WidgetContent dispatcher, HeaderRow, BulletRow, LoadingContent, EmptyContent, ErrorContent, DocumentNotFoundContent, SessionExpiredContent"
      min_lines: 120
    - path: "android/app/src/main/java/com/gmaingret/notes/widget/RetryActionCallback.kt"
      provides: "ActionCallback that triggers NotesWidget().update(context, glanceId)"
      min_lines: 8
    - path: "android/app/src/main/java/com/gmaingret/notes/widget/ReconfigureActionCallback.kt"
      provides: "ActionCallback that opens WidgetConfigActivity for reconfiguration"
      min_lines: 8
  key_links:
    - from: "WidgetContent.kt"
      to: "WidgetUiState"
      via: "when(uiState) dispatch to composable per state"
      pattern: "when.*uiState"
    - from: "WidgetContent.kt HeaderRow"
      to: "MainActivity"
      via: "actionStartActivity intent with OPEN_DOCUMENT_ID extra"
      pattern: "OPEN_DOCUMENT_ID"
    - from: "NotesWidget.kt provideContent"
      to: "WidgetContent composable"
      via: "provideContent { GlanceTheme { WidgetContent(uiState) } }"
      pattern: "WidgetContent"
---

<objective>
Implement all Glance composable UI for the widget: header row with document title, bullet list with formatting, and all display states (loading, empty, error, document not found, session expired). Wire the composables into NotesWidget.provideContent.

Purpose: This is the visual rendering layer that makes the widget useful. After this plan, users see actual document content in the widget with correct theming and all edge-case states handled.

Output: Complete widget UI rendering with all states; visual checkpoint for user verification on device.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-widget-foundation/13-CONTEXT.md
@.planning/phases/13-widget-foundation/13-RESEARCH.md
@.planning/phases/13-widget-foundation/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From widget/WidgetUiState.kt (created in Plan 01):
```kotlin
sealed interface WidgetUiState {
    data object Loading : WidgetUiState
    data object NotConfigured : WidgetUiState
    data object Empty : WidgetUiState
    data class Content(val documentTitle: String, val bullets: List<WidgetBullet>) : WidgetUiState
    data class Error(val message: String) : WidgetUiState
    data object DocumentNotFound : WidgetUiState
    data object SessionExpired : WidgetUiState
}

data class WidgetBullet(val id: String, val content: String, val isComplete: Boolean)
```

From widget/NotesWidgetColorScheme.kt (created in Plan 01):
```kotlin
object NotesWidgetColorScheme {
    val colors: ColorProviders  // wraps LightColorScheme + DarkColorScheme
}
```

From widget/NotesWidget.kt (created in Plan 01):
```kotlin
class NotesWidget : GlanceAppWidget() {
    override val sizeMode = SizeMode.Responsive(setOf(SMALL_SIZE, MEDIUM_SIZE, LARGE_SIZE))
    override suspend fun provideGlance(context: Context, id: GlanceId) {
        // ... fetches data, then provideContent { ... }
    }
}
```

From presentation/theme/Color.kt (existing):
```kotlin
val LightColorScheme = lightColorScheme(...)
val DarkColorScheme = darkColorScheme(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widget Glance composables and action callbacks</name>
  <files>
    android/app/src/main/java/com/gmaingret/notes/widget/WidgetContent.kt,
    android/app/src/main/java/com/gmaingret/notes/widget/RetryActionCallback.kt,
    android/app/src/main/java/com/gmaingret/notes/widget/ReconfigureActionCallback.kt,
    android/app/src/main/java/com/gmaingret/notes/widget/NotesWidget.kt,
    android/app/src/test/java/com/gmaingret/notes/widget/WidgetContentHelperTest.kt
  </files>
  <behavior>
    - stripMarkdownSyntax("hello #tag @mention !!date **bold**") returns "hello bold" (strips tags/mentions/dates, strips ** markers but keeps bold text)
    - stripMarkdownSyntax("normal text") returns "normal text" unchanged
    - stripMarkdownSyntax("~~strikethrough~~") returns "strikethrough" (strip markers, isComplete handles visual)
    - stripMarkdownSyntax("") returns ""
  </behavior>
  <action>
1. Create `WidgetContent.kt` with all Glance composables. CRITICAL: Import only from `androidx.glance.*` packages -- NOT `androidx.compose.*`. Key composables:

   **WidgetContent(uiState: WidgetUiState, context: Context)**: Top-level dispatcher. Wraps everything in a Glance `Column` with `GlanceModifier.fillMaxSize().background(GlanceTheme.colors.surface).cornerRadius(16.dp)`. Uses `when(uiState)` to dispatch to the appropriate composable.

   **HeaderRow(title: String, documentId: String, context: Context)**: Row with:
   - Document title: `Text` with `GlanceModifier.defaultWeight()`, maxLines=1, `TextStyle(fontSize=16.sp, fontWeight=FontWeight.Medium, color=GlanceTheme.colors.onSurface)`. Clickable via `actionStartActivity(Intent(context, MainActivity::class.java).apply { flags = FLAG_ACTIVITY_NEW_TASK or FLAG_ACTIVITY_CLEAR_TOP; putExtra("OPEN_DOCUMENT_ID", documentId) })`.
   - [+] button placeholder: `Text("+")` with `GlanceModifier.padding(8.dp).clickable(...)`. For Phase 13, make it a no-op or open the app. Phase 15 will wire it to the add-bullet overlay.
   - Thin divider below: `Box(GlanceModifier.fillMaxWidth().height(1.dp).background(GlanceTheme.colors.outlineVariant))`

   **BulletRow(bullet: WidgetBullet, context: Context, documentId: String)**: Row with:
   - Bullet dot: `Box(GlanceModifier.size(6.dp).cornerRadius(3.dp).background(...))` -- use `GlanceTheme.colors.primary` for active, `ColorProvider(Color(0xFF9CA3AF))` for completed
   - Spacer 8.dp
   - Text: maxLines=1, fontSize=14.sp. For completed: `TextDecoration.LineThrough` + `ColorProvider(Color(0xFF9CA3AF))`. For active: `GlanceTheme.colors.onSurface`, `TextDecoration.None`.
   - Delete x placeholder: `Text("x")` styled same color as text, right-aligned via `GlanceModifier.defaultWeight()` on the text. Phase 15 will wire this. For now, no-op clickable or omit (since Phase 15 adds it).
   - Entire row clickable to open app: `actionStartActivity` with OPEN_DOCUMENT_ID.
   - Horizontal padding: 12.dp, vertical: 4.dp

   **ContentView(state: WidgetUiState.Content, context: Context)**: Column with HeaderRow + LazyColumn of BulletRows. LazyColumn uses `items(state.bullets.size) { index -> BulletRow(state.bullets[index], ...) }`.

   **LoadingContent()**: Column with 4 static placeholder rows. Each row: gray dot (6.dp, Color 0xFFD1D5DB) + spacer + gray rectangle (12.dp height, alternating 180.dp/120.dp width, same gray). No animation (Glance does not support it per research). Add a subtle header placeholder too (taller gray bar).

   **EmptyContent()**: Centered `Text("No bullets yet", style = TextStyle(fontSize = 14.sp, color = GlanceTheme.colors.onSurfaceVariant))`.

   **ErrorContent(message: String)**: Centered `Text("Couldn't load -- tap to retry")` wrapped in `Box(GlanceModifier.fillMaxSize().clickable(actionRunCallback<RetryActionCallback>()))`. Use GlanceTheme.colors.error for text color.

   **DocumentNotFoundContent()**: Centered text "Document not found" + "Tap to reconfigure" wrapped in clickable Box that opens ReconfigureActionCallback.

   **SessionExpiredContent()**: Centered text "Session expired" + "Tap to sign in" wrapped in clickable Box that opens the main app (actionStartActivity to MainActivity).

2. Create `RetryActionCallback.kt`: `class RetryActionCallback : ActionCallback` -- in `onAction`, call `NotesWidget().update(context, glanceId)` to trigger a re-render (provideGlance re-fetches data).

3. Create `ReconfigureActionCallback.kt`: `class ReconfigureActionCallback : ActionCallback` -- in `onAction`, get the Android appWidgetId from the GlanceId using `GlanceAppWidgetManager(context).getAppWidgetId(glanceId)` (this is the correct Glance 1.1 API -- note: NOT `getId()`). Then launch `WidgetConfigActivity` with that appWidgetId as an extra via `context.startActivity(Intent(...))`.

4. Update `NotesWidget.kt` provideContent to use `WidgetContent(uiState = uiState, context = context)` instead of placeholder text.

5. Extract a `stripMarkdownSyntax(content: String): String` helper function (can be in WidgetContent.kt or a separate file). This strips:
   - `#tag` patterns (word starting with #): regex `#\w+`
   - `@mention` patterns: regex `@\w+`
   - `!!date` patterns: regex `!![^\s]+`
   - Bold markers `**text**` -> keep "text": regex `\*\*(.*?)\*\*` -> `$1`
   - Strikethrough markers `~~text~~` -> keep "text": regex `~~(.*?)~~` -> `$1`
   - Trim extra whitespace
   This was already referenced in Plan 01's fetchWidgetData -- make sure both plans use the same function. If Plan 01 already created this in NotesWidget.kt, move it to WidgetContent.kt and have NotesWidget import it.

6. Write `WidgetContentHelperTest.kt`: Unit tests for `stripMarkdownSyntax` -- pure Kotlin, no Robolectric needed. Test cases per the behavior block above.
  </action>
  <verify>
    <automated>ssh root@192.168.1.50 "cd /root/notes/android && ./gradlew testDebugUnitTest --tests 'com.gmaingret.notes.widget.*' -x lintDebug 2>&1 | tail -20"</automated>
  </verify>
  <done>All Glance composables implemented for every WidgetUiState variant. RetryActionCallback and ReconfigureActionCallback wired. NotesWidget.provideContent uses WidgetContent. stripMarkdownSyntax tests pass. Project compiles with assembleDebug.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of widget on device</name>
  <what-built>Complete widget with document picker config activity, all display states (content, loading, empty, error, document not found, session expired), Material 3 theming with dark/light mode, header with document title, scrollable bullet list with completed bullet styling.</what-built>
  <how-to-verify>
    1. Build and install: `cd android && ./gradlew assembleDebug` then `adb install -r app/build/outputs/apk/debug/app-debug.apk`
    2. Long-press home screen -> select Notes widget -> verify document picker opens
    3. Pick a document -> verify widget shows document title in header + root bullets in list
    4. Verify completed bullets show strikethrough + muted color
    5. Verify long text truncates with ellipsis
    6. Tap document title in widget header -> verify full app opens to that document
    7. Long-press widget -> verify "Reconfigure" option appears -> tap it -> verify picker re-opens
    8. Toggle system dark mode -> verify widget follows the theme change
    9. Resize the widget -> verify content adapts (more rows visible in taller widget)
    10. Test with a document that has no bullets -> verify "No bullets yet" centered text
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>Build APK, install on device via adb, and present to user for visual verification of all widget states.</action>
  <verify>User approves widget appearance and functionality.</verify>
  <done>User has confirmed the widget renders correctly in all states on a real device.</done>
</task>

</tasks>

<verification>
- `./gradlew assembleDebug -x lintDebug` compiles successfully
- `./gradlew testDebugUnitTest --tests "com.gmaingret.notes.widget.*" -x lintDebug` passes all tests
- Widget visually shows all states correctly on a real device
- Widget follows system dark/light mode
- Tapping document title opens the app
</verification>

<success_criteria>
- Widget header shows document title (truncated, left-aligned) + [+] placeholder
- Bullet list scrolls with bullet dot + text format
- Completed bullets have strikethrough and muted color
- All 7 display states render correctly (Loading, NotConfigured, Empty, Content, Error, DocumentNotFound, SessionExpired)
- Error and DocumentNotFound states respond to tap (retry / reconfigure)
- Dark mode automatically followed
- User has approved the visual result on device
</success_criteria>

<output>
After completion, create `.planning/phases/13-widget-foundation/13-03-SUMMARY.md`
</output>
