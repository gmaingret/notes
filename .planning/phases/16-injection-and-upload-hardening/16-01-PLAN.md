---
phase: 16-injection-and-upload-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/searchService.ts
  - server/src/services/tagService.ts
  - server/src/routes/attachments.ts
  - server/tests/search.test.ts
  - server/tests/attachments-svg.test.ts
autonomous: true
requirements: [INJ-01, INJ-02, INJ-03, UPLD-02]
must_haves:
  truths:
    - "Searching for text containing % or _ returns only literal matches"
    - "Navigating to a tag containing % or _ shows only bullets tagged with that exact tag"
    - "Downloading an SVG triggers a file-save dialog instead of rendering in the browser"
    - "Filenames with special characters in Content-Disposition are sanitized"
  artifacts:
    - path: "server/src/services/searchService.ts"
      provides: "ILIKE-safe search with escaped metacharacters"
      contains: "escapeIlike"
    - path: "server/src/services/tagService.ts"
      provides: "ILIKE-safe tag lookup with escaped metacharacters"
      contains: "escapeIlike"
    - path: "server/src/routes/attachments.ts"
      provides: "SVG force-download via Content-Disposition: attachment, filename sanitization"
      contains: "attachment"
    - path: "server/tests/search.test.ts"
      provides: "Tests for ILIKE escaping"
    - path: "server/tests/attachments-svg.test.ts"
      provides: "Tests for SVG Content-Disposition and filename sanitization"
  key_links:
    - from: "server/src/services/searchService.ts"
      to: "drizzle ilike()"
      via: "escapeIlike helper applied to user query before pattern interpolation"
      pattern: "escapeIlike"
    - from: "server/src/services/tagService.ts"
      to: "drizzle ilike()"
      via: "escapeIlike helper applied to tag value before pattern interpolation"
      pattern: "escapeIlike"
    - from: "server/src/routes/attachments.ts"
      to: "Content-Disposition header"
      via: "SVG check on mimeType before setting disposition"
      pattern: "svg.*attachment"
---

<objective>
Escape ILIKE metacharacters in search and tag queries, and force SVG downloads to prevent stored XSS.

Purpose: Prevent query injection via % and _ wildcards in user-controlled search input, and prevent stored XSS via uploaded SVG files rendered inline in the browser.
Output: Patched searchService, tagService, attachments route, and tests validating the fixes.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-injection-and-upload-hardening/16-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From server/src/services/searchService.ts:
```typescript
export async function searchBullets(
  userId: string,
  query: string
): Promise<Array<{ id: string; content: string; documentId: string; documentTitle: string }>>;
// Currently builds pattern as `%${normalized}%` — no escaping of % or _ in normalized
```

From server/src/services/tagService.ts:
```typescript
export async function getBulletsForTag(
  userId: string,
  chipType: ChipType,
  value: string
): Promise<Array<{ id: string; content: string; documentId: string; documentTitle: string }>>;
// Currently builds pattern like `%#${value}%` — no escaping of % or _ in value
```

From server/src/routes/attachments.ts:
```typescript
// GET /:id/file — currently sets Content-Disposition: inline for ALL files including SVG
res.setHeader('Content-Disposition', `inline; filename="${attachment.filename}"`);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ILIKE metacharacter escaping in search and tag services</name>
  <files>server/src/services/searchService.ts, server/src/services/tagService.ts, server/tests/search.test.ts</files>
  <behavior>
    - Test: escapeIlike('%hello_world%') returns '\%hello\_world\%' (backslash-escaped)
    - Test: searchBullets builds pattern with escaped user input (% and _ are literal)
    - Test: getBulletsForTag builds pattern with escaped tag value (% and _ are literal)
  </behavior>
  <action>
    Create a shared helper function `escapeIlike(input: string): string` that replaces `%` with `\%` and `_` with `\_` (PostgreSQL ILIKE escape syntax using backslash). Place it in a new file `server/src/services/utils/escapeIlike.ts` or inline in both services — use discretion.

    In `searchService.ts`:
    - Import escapeIlike
    - Change line 17 from `const pattern = \`%${normalized}%\`` to `const pattern = \`%${escapeIlike(normalized)}%\``

    In `tagService.ts` (`getBulletsForTag`):
    - Import escapeIlike
    - Apply escapeIlike to `value` before interpolating into the ILIKE pattern on lines 58-63
    - Example: `pattern = \`%#${escapeIlike(value)}%\``

    Create `server/tests/search.test.ts` with unit tests:
    - Test the escapeIlike helper directly with %, _, and combined input
    - Test that searchBullets and getBulletsForTag call ilike with escaped patterns (mock db)

    Note: PostgreSQL ILIKE uses backslash as the default escape character, so `\%` matches a literal `%`.
  </action>
  <verify>
    <automated>ssh root@192.168.1.50 "cd /root/notes/server && npx vitest run tests/search.test.ts 2>&1 | tail -20"</automated>
  </verify>
  <done>escapeIlike helper exists and is applied in both searchService.searchBullets and tagService.getBulletsForTag; tests pass confirming % and _ are escaped before ILIKE pattern construction</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SVG force-download via Content-Disposition: attachment</name>
  <files>server/src/routes/attachments.ts, server/tests/attachments-svg.test.ts</files>
  <behavior>
    - Test: GET /:id/file for an SVG attachment returns Content-Disposition: attachment
    - Test: GET /:id/file for a PNG attachment returns Content-Disposition: inline
    - Test: Filename containing double quotes is sanitized (quotes replaced with single quotes)
    - Test: Filename containing newlines or control characters is stripped
  </behavior>
  <action>
    In the GET `/:id/file` route handler in `server/src/routes/attachments.ts` (line 103-108):

    Change the Content-Disposition logic from always `inline` to checking the MIME type:
    - If `attachment.mimeType` is `image/svg+xml`, set `Content-Disposition: attachment; filename="..."` (forces download)
    - For all other types, keep `Content-Disposition: inline; filename="..."` (current behavior unchanged)

    Per user decision: no SVG sanitization needed — force-download is the chosen approach.

    Also sanitize the filename in Content-Disposition to prevent header injection (UPLD-02):
    - Replace `"` (double quotes) with `'` (single quote)
    - Strip newlines (`\r`, `\n`) and control characters (chars 0x00-0x1F)
    - This applies to ALL attachments, not just SVGs

    The sanitization belongs here because this is the only place filenames appear in HTTP headers.

    Create `server/tests/attachments-svg.test.ts` with tests for:
    - SVG file returns Content-Disposition: attachment header
    - Non-SVG file returns Content-Disposition: inline header
    - Filename with double quotes is sanitized
    - Filename with newlines/control chars is stripped
  </action>
  <verify>
    <automated>ssh root@192.168.1.50 "cd /root/notes/server && npx vitest run tests/attachments-svg.test.ts 2>&1 | tail -20"</automated>
  </verify>
  <done>SVG files served with Content-Disposition: attachment; all other files remain inline; filenames are sanitized in the Content-Disposition header (no quotes, newlines, or control chars); tests in attachments-svg.test.ts pass</done>
</task>

</tasks>

<verification>
1. Deploy to server and search for a literal `%` character — should return only bullets containing `%`, not all bullets
2. Navigate to a tag panel entry containing `_` — should show only matching bullets
3. Upload an SVG file, then click to download it — browser should trigger a save dialog, not render the SVG
</verification>

<success_criteria>
- escapeIlike helper escapes % and _ with backslash before ILIKE pattern construction
- searchBullets and getBulletsForTag both use escapeIlike on user-provided input
- SVG files are served with Content-Disposition: attachment
- Non-SVG files continue to be served inline (no behavioral change)
- Content-Disposition filenames are sanitized against injection
- All tests pass (search.test.ts and attachments-svg.test.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/16-injection-and-upload-hardening/16-01-SUMMARY.md`
</output>
