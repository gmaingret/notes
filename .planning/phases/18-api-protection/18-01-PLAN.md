---
phase: 18-api-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/app.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "Sending more than 100 requests to /api/bullets in 15 minutes returns 429 Too Many Requests"
    - "Normal in-app usage continues to work without any visible change"
    - "API-02 is documented as resolved-by-design in REQUIREMENTS.md"
    - "ROADMAP success criteria reflects Bearer token CSRF mitigation instead of CSRF token enforcement"
  artifacts:
    - path: "server/src/app.ts"
      provides: "dataLimiter rate limiter for all data endpoints"
      contains: "dataLimiter"
    - path: ".planning/REQUIREMENTS.md"
      provides: "API-02 marked resolved-by-design"
      contains: "resolved-by-design"
    - path: ".planning/ROADMAP.md"
      provides: "Updated success criteria for phase 18"
  key_links:
    - from: "server/src/app.ts"
      to: "data route handlers"
      via: "app.use with dataLimiter middleware applied before route mounting"
      pattern: "app\\.use.*dataLimiter"
---

<objective>
Add per-IP rate limiting to all data endpoints and document CSRF as resolved-by-design.

Purpose: Protect data endpoints against brute-force abuse (data exfiltration, DoS) and close the CSRF audit finding with proper justification.
Output: Updated app.ts with data rate limiter, updated REQUIREMENTS.md and ROADMAP.md reflecting API-02 design decision.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/src/app.ts
@server/src/index.ts

<interfaces>
<!-- Existing rate limiter pattern in app.ts that Task 1 will replicate -->

From server/src/app.ts:
```typescript
import rateLimit from 'express-rate-limit';

// Existing pattern — authLimiter (20/15min) and refreshLimiter (60/15min)
const authLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 20,
  standardHeaders: true,
  legacyHeaders: false,
});
app.use('/api/auth/login', authLimiter);
```

From server/src/index.ts (data routes that need limiting):
```typescript
app.use('/api/documents', documentsRouter);
app.use('/api/bullets', bulletsRouter);
app.use('/api', undoRouter);           // mounts /api/undo
app.use('/api/bookmarks', bookmarksRouter);
app.use('/api/tags', tagsRouter);
app.use('/api/search', searchRouter);
app.use('/api/attachments', attachmentsRouter);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data endpoint rate limiter</name>
  <files>server/src/app.ts</files>
  <action>
Add a `dataLimiter` rate limiter in `app.ts` createApp(), placed after the existing refreshLimiter block and before the health check. Configuration:

- `windowMs: 15 * 60 * 1000` (15 minutes)
- `max: 100` (100 requests per window per IP)
- `standardHeaders: true, legacyHeaders: false` (same as existing limiters)

Apply it to each data route prefix individually (same pattern as authLimiter):
- `app.use('/api/documents', dataLimiter);`
- `app.use('/api/bullets', dataLimiter);`
- `app.use('/api/undo', dataLimiter);`
- `app.use('/api/bookmarks', dataLimiter);`
- `app.use('/api/tags', dataLimiter);`
- `app.use('/api/search', dataLimiter);`
- `app.use('/api/attachments', dataLimiter);`

Add a comment above the block: `// Rate limit data endpoints (abuse / exfiltration protection)`

Do NOT apply to `/api/auth/*` (already has its own stricter limiters) or `/health`.

Note: The limiter middleware is registered in app.ts (before routes mount in index.ts). When index.ts calls `app.use('/api/bullets', bulletsRouter)`, Express evaluates middleware in registration order, so the dataLimiter registered first in app.ts will run before the route handler. This works because Express matches middleware by path prefix regardless of registration order relative to route handlers.
  </action>
  <verify>
    <automated>grep -c 'dataLimiter' server/src/app.ts</automated>
  </verify>
  <done>app.ts contains dataLimiter with max:100/15min applied to all 7 data route prefixes. Existing auth limiters unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document CSRF as resolved-by-design and update roadmap</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
**REQUIREMENTS.md changes:**
1. Change the API-02 line from:
   `- [ ] **API-02**: CSRF token is required on state-changing endpoints as defense-in-depth alongside SameSite cookies`
   to:
   `- [x] **API-02**: CSRF protection resolved by design — all data endpoints use Bearer token auth (Authorization header), which browsers never auto-send on cross-origin requests. The only cookie-authenticated endpoint (POST /api/auth/refresh) uses SameSite=Strict. No CSRF token implementation needed.`

2. In the Traceability table, change API-02 status from `Pending` to `Resolved (by design)`

**ROADMAP.md changes:**
1. In Phase 18 Success Criteria, change item 2 from:
   `2. A state-changing request (create/update/delete) without a valid CSRF token is rejected`
   to:
   `2. CSRF is mitigated by design: all data endpoints require Bearer token auth (not auto-sent by browsers), and the refresh endpoint uses SameSite=Strict cookies`

2. In Phase 18 description line, change:
   `Add rate limiting across data endpoints, add CSRF token enforcement on state-changing endpoints`
   to:
   `Add rate limiting across data endpoints, document CSRF mitigation by Bearer token architecture`

3. Fix the Phase 18 Plans list (currently shows phase 17 plans by mistake). Replace with:
   ```
   Plans:
   - [ ] 18-01-PLAN.md — Rate limiting and CSRF documentation
   ```

4. Update plan count: `**Plans**: 1 plan`

Also add a brief code comment in `server/src/app.ts` after the dataLimiter block (from Task 1):
```
// CSRF note: No CSRF token middleware needed. All data endpoints require
// Bearer token auth via Authorization header, which browsers never attach
// automatically to cross-origin requests. See REQUIREMENTS.md API-02.
```
  </action>
  <verify>
    <automated>grep -c "resolved by design" .planning/REQUIREMENTS.md && grep -c "Bearer token" .planning/ROADMAP.md && grep -c "CSRF note" server/src/app.ts</automated>
  </verify>
  <done>API-02 marked resolved-by-design in REQUIREMENTS.md with justification. ROADMAP success criteria updated to reflect Bearer token CSRF mitigation. Phase 18 plan list corrected. CSRF design comment added to app.ts.</done>
</task>

</tasks>

<verification>
1. `grep 'dataLimiter' server/src/app.ts` shows limiter definition and 7 route applications
2. `grep 'API-02' .planning/REQUIREMENTS.md` shows resolved-by-design status
3. ROADMAP Phase 18 success criteria no longer mentions "CSRF token is rejected"
4. Deploy to server, send rapid requests to confirm 429 response after limit exceeded
</verification>

<success_criteria>
- dataLimiter (100/15min per IP) applied to all 7 data route prefixes in app.ts
- Auth route limiters unchanged (20/15min for login/register, 60/15min for refresh)
- API-02 documented as resolved-by-design with Bearer token justification
- ROADMAP success criteria updated to reflect design decision
- Normal app usage unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/18-api-protection/18-01-SUMMARY.md`
</output>
